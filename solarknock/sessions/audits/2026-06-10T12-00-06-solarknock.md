---
schema_version: 1
audit_id: 2026-06-10T12-00-06-solarknock
product: solarknock
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/ab8aa10e-d5b2-4358-9c38-dd8d81b4053f/subagents/agent-a014e224b1dbd9375.jsonl
  session_started: '2026-06-08T19:00:26.781Z'
  session_ended: '2026-06-08T19:03:57.951Z'
  session_size_bytes: 217026
  truncated: false
---

## Decisions made

No final decisions were made during this session. The session consisted entirely of an adversarial design review commissioned by the operator. All conclusions are recommendations, not operator-confirmed decisions.

## Open questions

1. Whether to split the API key from the manager invite code (spec Decision #1) — the reviewing agent recommends splitting immediately; the spec recommends deferring. No operator ruling recorded.
2. Whether the API panel in the settings hub should use inline sections or sub-routes (spec Decision #2) — the reviewing agent recommends sub-routes for TEAM/ORGANIZATION tiers and inline sections for ME only. No operator ruling recorded.
3. Whether the spec should be revised before implementation or taken as-is — the reviewer assessed the spec as "needs rework before implementation," but no operator confirmation of that verdict was recorded.
4. Whether to add a dedicated `apiKey` column to the database to replace the shared `manager_code` as the API credential — raised as a recommendation, not decided.
5. The rotation feature promised in customer-facing documentation does not exist in code; no decision recorded on whether to build it, remove the documentation promise, or add a warning.

## People mentioned

No individuals by name were mentioned in this session. Brighthouse was referenced as an organization (write-API-enabled customer).

## Dates and deadlines referenced

- 2026-05-27: referenced as the date of the "IA refactor phase 3" comment in `account.tsx`, which folded `/closer-settings` into `/account` — this work is already shipped.
- No deadlines or target dates for the proposed consolidation work were stated.

## Thoughts not yet in the canvas

- The `/closer-settings` route is already a dead redirect (shipped in a prior refactor, per code comments dated 2026-05-27). The spec presents folding it in as pending work; the canvas should reflect it as completed.
- Google Calendar OAuth uses `/closer-settings` as its server-side default `returnPath` in six locations (`server/routes/auth.ts`). This is an undocumented server-side dependency on the "dead" route that must be updated before any route cleanup proceeds.
- The `admin-settings.tsx` history/publish/restore tab (solar-estimate version history with publish and restore capabilities) is absent from the proposed IA in the spec and has no milestone covering it.
- Auto-created disabled users accumulate in production whenever the reporting write API receives an unknown `user_email`. Customer-facing documentation promises an admin panel to list, activate, promote, and delete these users. No such UI exists and no milestone covers building it.
- The code-rotation feature promised in customer-facing documentation (`api.html`) has never been implemented anywhere in the codebase — no endpoint, no UI, for any invite code type.
- The `manager_code` value (which serves as the API bearer token) is displayed in plaintext to managers (not only admins) on the org-members screen. The proposed admin-only API tier in the spec is inconsistent with this existing manager-visible surface.
- `getOrgByManagerCode` uppercases the input code on lookup; the API panel's key display and any documentation examples must reflect that codes are case-insensitive on input and stored in uppercase.
- The `Stack.Screen` entries for `manage-users` and `manage-teams` in `app/_layout.tsx` are not mentioned in the spec's cleanup section and must be removed alongside the route files.
- The billing route has no client-side role gate — any authenticated user can navigate to it and receives a 403 on the data fetch, resulting in an error state. The spec's characterization as "admin (effectively)" is accurate but the UX gap (visible error for non-admins) is not addressed.

## Status changes implied

No status changes to existing canvas milestones are implied by this session. The session was a review, not an implementation or operator instruction to advance work.

