---
schema_version: 1
audit_id: 2026-06-10T12-00-04-solarknock
product: solarknock
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/ab8aa10e-d5b2-4358-9c38-dd8d81b4053f.jsonl
  session_started: '2026-04-15T21:06:50.160Z'
  session_ended: '2026-06-10T18:35:18.185Z'
  session_size_bytes: 140305464
  truncated: true
---

## Decisions made

The operator decided to add a conversations stat to both `/dashboard` and `/team` leaderboard screens, using the same definition as the green map pill (status NOT IN `not_home`, `failed_knock`). The operator confirmed the `totalConversations` field should be a new column named "Convos" on the team leaderboard, and a new tile on the dashboard StatCard grids.

The operator decided to drop role-based restrictions on the leaderboard so every org member sees full team totals and top performers, not just managers.

The operator decided to refetch leaderboard data on screen focus (`useFocusEffect`) and also refetch territories on focus, but explicitly opted out of background/foreground refresh.

The operator decided to add a new multi-value "Actions Taken" field to knock pins. Storage shape chosen: a dedicated `actions_taken text[]` column (not JSONB). Chips appear above the note field in AddressPopup. Initial values: Price Estimate and Proposal Given.

The operator decided to also add "Actions Taken" to leads (with an additional value: Qualified) on the `/add-appointment-lead` page. Lead actions column is also named `actions_taken`. Both knock and lead actions are available via read and write APIs.

The operator decided to remove "Qualified" and "Proposal Sent" from the lead status pick list (old data remains queryable but those values no longer appear as options).

The operator confirmed docs-first policy: API documentation was written and deployed before implementation of the lead actions feature.

The operator decided the "Qualified" action value is leads-only; knock actions remain `{price_estimate, proposal_given}` only.

The operator requested saving session context to memory files before a server restart, then ended the session.

## Open questions

The operator asked about a model called "Fable 5" from Anthropic. The assistant could not confirm the existence of this model and noted it does not match Anthropic's known naming conventions. This remains unresolved — the operator did not clarify further.

The admin-settings consolidation spec (v2) is awaiting three operator decisions before implementation proceeds:
1. Whether to split the API key from the manager invite code (recommended by the reviewer).
2. Confirm sub-routes for TEAM/ORG screens vs inline ME.
3. Scope of v1 API panel: full (rotate + ghost-users + write feed) or staged (ship toggles + key first).

The operator did not answer whether to capture a Playwright screenshot of the live `/add-appointment-lead` actions chips.

The operator did not answer whether to run an end-to-end API round-trip test for `actions_taken` against prod using Brighthouse's write-enabled key.

## People mentioned

No individuals other than the operator are named in this session segment.

## Dates and deadlines referenced

2026-04-11: Original Twilio A2P campaign submission date (referenced in prior context summary).
2026-04-14: Most recent knock recorded in Brighthouse Solar org (referenced while diagnosing empty Knock List).
2026-04-15: Date of the prior session's Knock List and server fixes (referenced in code parity audit).
2026-04-16: Date server fixes (SPA HEAD support, `dailyKnockNumber`) were deployed to prod (referenced in parity matrix).
2026-04-23: Campaign resubmission date with HEAD fix and REVIEW01 instructions.
2026-04-24: Most recent Twilio campaign resubmission (after 30908 privacy policy rejection).
2026-04-24T03:53:55Z: Timestamp of Twilio campaign resubmission that was IN_PROGRESS at start of this segment.
2026-05-13: Referenced as when UI refactor Phase 1a paused.
2026-06-07: Deployment tag `prod-20260607-010102-4ce3cec` (team screen focus refetch).
2026-06-09: Deployment tags `prod-20260609-045845-b7e559d` (knock actions) and `prod-20260609-052740-412aefb` (lead actions).

No future deadlines or hard dates were set during this session.

## Thoughts not yet in the canvas

The operator asked whether a PWA installation can create phone notifications in the OS notification center. The assistant confirmed it is technically possible (Android always, iOS only when installed to Home Screen on 16.4+), but noted that SolarKnock's service worker is currently install-only with no push handlers, VAPID keys, or server-side push infrastructure. The operator did not commission a build; this remains unscoped.

The assistant flagged a gap: the public API docs at `solarknock.com/api` promise key rotation and auto-created-users management, but neither feature exists in the codebase. A "ghost user" problem was identified — the write API silently creates disabled user accounts that appear on leaderboards with no admin remedy.

A touch-hold threshold issue was flagged (600ms in `MapDisplay.web.tsx` is too long for reps; suggested fix: drop to 350–400ms plus a visual ring). Estimated ~30 min fix, noted as real user pain. Not commissioned.

An appointment disposition gap was fully documented: no UI or API currently allows marking an appointment as sat, no-show, or cancelled. `server_appointments.status` has `pending`/`completed`/`cancelled` columns but nothing ever writes them. The operator asked about no-show support only in a "report only" context — no implementation was requested.

The `reporting_api_write_enabled` flag has no UI and no API endpoint — it is SQL-only. The public docs describe a toggle that does not exist. This was documented in spec v2 but not yet built.

Two stale unmerged branches (`handoff/2026-04-11-consent-tz-fixes`, `sprint/mobile-arch-2026-04-11`) were noted as existing and not blocking anything.

Several untracked project files remain uncommitted: `.spyglass/`, `agents/`, `architecture/`, `CLAUDE.md`, `workplan.md`, `docs/MOBILE_UX_SPRINT_2026-04-11.md`, `docs/RELEASE_PLAN_IMPROVEMENT.md`. Operator was informed but no action was requested.

There is no GitHub Actions CI pipeline. Deployment is manual via `scripts/deploy-production.sh`. This was noted as a gap (prod and git can drift) but not acted upon per operator instruction.

The `PIN_UX_UNBUILT` memory note references a spec for Closer/Lead pins to share follow-up's note+schedule UX — spec exists, code does not. Not touched this session.

Error catchers to `@SNBigMonBot` (pending task #61) mentioned as an unfinished item. Not addressed.

Existing users connected under `calendar.readonly` must disconnect and reconnect to upgrade to `calendar.events`. Only informational; no action taken.

## Status changes implied

No named canvas track/milestone slugs appear in the session. The following state changes are implied from the work done:

- Twilio A2P campaign: FAILED (30908 privacy policy) → resubmitted as IN_PROGRESS (as of 2026-04-24T03:53:55Z, per prior context). No further status update occurred in this session segment.
- Google Calendar OAuth verification: APPROVED (both `calendar.events` and `calendar.readonly` scopes). Flag `GOOGLE_CALENDAR_EVENTS_ENABLED=true` was set in production. No new changes this session.
- Conversations stat feature: not started → designed, implemented, committed, and deployed to production (`prod-20260607-010102-4ce3cec` or later).
- Team screen data freshness: stale-on-tab-return → refetches on focus (leaderboard + territories), deployed `prod-20260607-010102-4ce3cec`.
- Knock "Actions Taken" field: not started → migration applied, full-stack implementation deployed `prod-20260609-045845-b7e559d`.
- Lead "Actions Taken" field: not started → migration applied, full-stack implementation with API read+write deployed `prod-20260609-052740-412aefb`.
- Lead status pick list: `qualified` and `proposal_sent` removed from picker options (old data unaffected), deployed same tag.
- Code parity (local/GitHub/prod): diverged → reconciled. Three commits pushed to `origin/main`; web client rebuilt and deployed; prod confirmed at `281ee8a`.
- Admin-settings consolidation spec: v1 drafted → design-review subagent dispatched → v2 committed (`8fae053`). Awaiting operator decisions before implementation.

