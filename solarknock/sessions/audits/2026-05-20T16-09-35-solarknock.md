---
schema_version: 1
audit_id: 2026-05-20T16-09-35-solarknock
product: solarknock
generated_by: claude-sonnet-4-6
generated_at: '2026-05-20T16:08:00Z'
sources_summarized:
- kind: claude_session
  path: /home/readystack/.claude/projects/-home-readystack/ab8aa10e-d5b2-4358-9c38-dd8d81b4053f.jsonl
  session_started: '2026-04-15T21:06:50.160Z'
  session_ended: '2026-05-20T13:17:48.336Z'
  session_size_bytes: 74878585
  truncated: true
---

# Session 2026-05-20 — Google Cloud OAuth cutover, location bug fix, and secrets playbook

## Decisions made

The operator decided to proceed immediately with the Google Cloud OAuth cutover to the new project (`solarknock`, project number 527200898067, under the `fablesoftware.us` org). The cutover was executed in the same session and confirmed live.

The operator decided that secrets currently in `ecosystem.config.cjs` (committed to git) should not be re-committed there; going forward, server-side secrets live in `/var/www/solarknock/.env` (chmod 600, not in git), loaded at server startup via `import "dotenv/config"`. The `ecosystem.config.cjs` file was updated to a minimal version (NODE_ENV and PORT only) and committed.

The operator directed that Stage 2 and Stage 3 of PWA offline support should be roadmapped and not deployed yet. Stage 1 (knock offline queue) was released separately in the same session.

Territory polygons on the map were changed from green (a temporary build marker) back to pink, per operator direction.

The operator decided to fix the map center-drag bug by adding `pointerEvents="box-none"` to the territory sidebar wrapper, following the operator's own diagnosis that an invisible full-width overlay was intercepting touches. The fix was confirmed working by the operator ("fixed it").

The location button fix (stale GPS coords sent all reps to the morning standup address) was approved and deployed immediately by operator instruction ("do the fix and push to prod please").

The operator decided to skip pre-cutover comms email to the 5 active Calendar users and proceed directly with the cutover; users will see a one-time re-authorize prompt on next Calendar use.

The operator decided not to rotate the already-leaked Stripe, database, and session secrets during this session, treating that as a deferred separate pass per secret.

## Open questions

Stage A4 follow-up for the old Google Cloud project (`solarknockcom`, project 82610159184) is pending: confirm zero traffic on the old project, verify no active Calendar refresh tokens tied to the old OAuth client, delete the old project (30-day recoverable window), and re-enable the org policy `iam.disableServiceAccountKeyCreation` on `fablesoftware.us`. No timeline was set.

Stage 2 (service-worker app-shell cache) and Stage 3 (cache persistence and territory address cache) of PWA offline support remain unbuilt. An open question on Stage 3 is whether the synchronous main-thread write of a large number of pins (Brighthouse has approximately 4,700) is a real-world cost; the earlier map jank was proven to be unrelated, but the question was not fully resolved.

The `ecosystem.config.cjs` `EXPO_PUBLIC_*` keys previously in the committed file are now baked into the web bundle at build time and visible in DevTools; their protection relies on API-key restrictions configured on the Google Cloud side (HTTP referrer and bundle ID), not file permissions. No action was taken to verify those restrictions are in place.

The `.env.production` file is tracked in git and contains secrets. The operator was informed; it was flagged in the secrets playbook Legacy table for a separate cleanup pass with no timeline set.

Feature-card screenshots for the `/join-beta` page (Territories and Solar Estimate cards) were not yet captured. The operator said "i will get you screenshots for those" in a prior segment.

## People mentioned

John (operator, owner of ReadyStack/Exceed.io, primary developer of SolarKnock).

The 5 active Google Calendar users on the SolarKnock platform who will need to re-authorize after the OAuth cutover: john@brighthouse.solar, john@thedezz.com, johntdavenport@exceed.io, joshuaedavid2001@gmail.com, tom@brighthouse.solar.

## Dates and deadlines referenced

2026-04-11: Original Twilio A2P campaign first submitted; remained IN_PROGRESS for days before failure.

2026-04-15: Session break / context reset (workplan.md saved for handoff).

2026-04-16: Twilio campaign resubmitted with REVIEW01 invite code and reviewer walkthrough instructions. Google Calendar OAuth scope (`calendar.events` and `calendar.readonly`) approved by Google's Third Party Data Safety Team. `GOOGLE_CALENDAR_EVENTS_ENABLED=true` set in production.

2026-04-23: Third Twilio resubmission after diagnosing HEAD-request returning 404; fix deployed (`server/index.ts` SPA fallback extended to accept HEAD).

2026-04-24: Fourth Twilio resubmission after diagnosing missing SMS section in `app.solarknock.com/privacy`; both privacy pages updated. Twilio A2P playbook created at `docs/TWILIO_A2P_PLAYBOOK.md`.

2026-05-05: `~/credentials/solarknock-keys/cutover.env` created (referenced in transcript as already staged when the Google auth cutover topic arose).

2026-05-13: Last stable app commit before the Stage 1+3 PWA deployment that the operator temporarily wanted to roll back; map gestures debugging traced to this period.

2026-05-14: Commit `0f546f4` added `touch-action: manipulation` to `components/MapDisplay.web.tsx`, which contributed to map gesture problems.

2026-05-19: Territory sidebar `pointerEvents="box-none"` fix deployed and confirmed working by operator. Pink territory color restored. Stage 1 (offline knock queue) redeployed.

2026-05-20 (this session): Current-location button fix deployed (commit `9583fa5`, release tag `prod-20260520-001927-9583fa5`). Google Cloud OAuth cutover executed and verified live (release tag `prod-20260520-130957-c3f8f42`).

Stage A4 old-project cleanup: no formal deadline set; informal note references a 30-day recoverable window on the old project deletion.

## Thoughts not yet in the canvas

The Twilio A2P campaign was resubmitted multiple times across the session period and remains the last status recorded as IN_PROGRESS as of 2026-04-24. No canvas milestone exists for this tracking.

The operator commented on the debugging approach during the map gesture investigation: "you just guessing is just bs tbh" and "this is taking too long." A feedback memory file (`feedback_instrument_dont_guess.md`) was written to capture the lesson: debug UI/behavior bugs by reproducing and instrumenting rather than guess-and-deploy.

The `ecosystem.config.cjs` on prod was already in the clean minimal form (NODE_ENV and PORT only) prior to this session — diverged from the committed repo version, which still had plain-text secrets. This divergence had been hiding silently. The session reconciled the committed file with the prod version.

The agent noted that `ecosystem.config.cjs` was committed to git in the old state with live secrets, and that `pm2 reload` with `--update-env` shadows dotenv variables (a gotcha that "already burned the team once in 2026-05" per the prod ecosystem file comment). A secrets playbook was written to document the correct pattern at `docs/SECRETS_PLAYBOOK.md`.

The `rotate-google-keys.sh` script had stale assumptions (referenced the wrong env file path and used the pm2 env-cache-gotcha invocation). The cutover was done manually rather than through that script.

The Knock List "Today" period showing empty while the Map tab shows pins is a UX mismatch that was explained (Map has no time filter; Today filter is correct but looks like a bug). The operator confirmed it resolved when switching to "This Month." No dedicated milestone exists for improving the empty-state UX on the Knock List.

The `/join-beta` marketing page feature cards need Territories and Solar Estimate screenshots. The operator said they would provide them. No milestone exists for this.

## Status changes implied

No canvas slugs are defined in §7 of the playbook, so no formal slug transitions can be recorded. The following state changes are implied by the session:

The Google Cloud OAuth cutover from project `solarknockcom` to project `solarknock` moved from staged/planned to complete and verified live.

Stage 1 PWA offline support (knock queue) moved from reverted/paused to re-deployed and live.

Stage 2 and Stage 3 PWA offline support moved from previously-deployed-then-reverted to explicitly roadmapped and not yet re-deployed.

The current-location button bug (all users sent to 4343 N College Ave, Fresno) moved from open to fixed and deployed (commit `9583fa5`).

The map center-drag bug (touches intercepted by territory sidebar wrapper) moved from open to fixed and deployed.

The secrets management pattern moved from ad hoc (plain-text secrets committed in `ecosystem.config.cjs`) to documented: server-side secrets in `/var/www/solarknock/.env`, not in git, `ecosystem.config.cjs` minimal.

