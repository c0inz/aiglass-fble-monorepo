---
schema_version: 1
audit_id: 2026-06-10T12-00-07-solarknock
product: solarknock
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/ab8aa10e-d5b2-4358-9c38-dd8d81b4053f/subagents/agent-ab17bf29e8e1d6a57.jsonl
  session_started: '2026-05-30T13:12:28.322Z'
  session_ended: '2026-05-30T13:24:37.986Z'
  session_size_bytes: 386551
  truncated: false
---

## Decisions made

The approved design spec (`2026-05-30-perf-eval-and-surface-suite-design.md`) was followed exactly for scaffolding Track B of the production surface performance test suite. No design decisions were delegated to the agent; all architectural choices were pre-approved.

Routes without a corresponding file in `app/` were dropped rather than using placeholder stubs: `leaderboard`, `org-management`, `set-appointment`, and `change-password` specs were not created.

The `playwright.config.ts` was not modified per the constraint that only the files listed in the spec may be touched (Track A/Track B conflict minimization). Per-test timeout overrides were applied instead.

The `window.__RQ_INFLIGHT__` instrumentation hook was gated on `NODE_ENV !== 'production'` OR `window.__PERF_INSTRUMENT__` OR query param `?perf=1`.

No new npm devDependencies were added; the suite relies solely on already-installed `@playwright/test`.

Cleanup of QA test data is performed via API calls only — no direct database credentials are used in the suite.

## Open questions

The `DELETE /api/qa/perf-runs/:runId` cleanup endpoint does not exist yet; 404 responses are swallowed and cleanup is best-effort. The endpoint needs to be built (likely Track A).

The `~/credentials/solarknock-qa.json` credentials file does not exist yet. Suite is blocked from running until `scripts/provision-qa-org.sh` is run and the file is created.

The `__RQ_INFLIGHT__` counter may need `?perf=1` appended to `goto()` URLs for surfaces to accurately capture RQ-settled time until the production flag is enabled.

The `assertSLO` function in `lib/slo.ts` does not emit a structured success log line, which means `perf-baseline-compute.js` can only extract samples from failing runs. A follow-up is noted to add a `console.log('[perf]…')` line for passing runs.

The global Playwright timeout of 30 seconds may trip surfaces other than `cold-app-open` if budgets are tight. Parent thread may need to bump the global timeout in `playwright.config.ts`.

## People mentioned

JD (referenced as "JD will tune" regarding initial SLO values).

## Dates and deadlines referenced

2026-05-30: date of the approved performance evaluation and surface suite design spec (`2026-05-30-perf-eval-and-surface-suite-design.md`).

## Thoughts not yet in the canvas

A `perf-format-alert.js` helper script was created to translate Playwright JSON output into Telegram alert bodies. No canvas milestone exists for this artifact.

A `perf-baseline-compute.js` script was created to aggregate per-surface p50/p95 across N baseline runs. No canvas milestone exists for this artifact.

The `docs/PERF_TEST_SUITE.md` operator guide was created covering: how to add a surface, update baselines, interpret failures, force a Telegram dry-run, and debug a single surface. No canvas milestone exists for this doc.

A `scripts/provision-qa-org.sh` script is referenced as a dependency but was not created in this session and does not exist in the repo. Its creation is not captured in the canvas.

The Telegram alert integration reuses the pattern from `server/services/telegram.ts` (referencing SNBigMonBot and a bot token read from the production environment). The bot token is read from the runtime environment and is not committed. Value redacted per §3.1.

## Status changes implied

No canvas slugs are present in the playbook (§7 canvas snapshot is empty). No existing slug status changes can be mapped.

