---
schema_version: 1
audit_id: 2026-06-24T02-35-02-spaiglass
product: spaiglass
generated_by: claude-sonnet-4-6
generated_at: '2026-05-29T18:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/johntdavenport/.claude/projects/-home-johntdavenport/418e4418-dfc2-4e7e-94cf-1a19549a29a8/subagents/workflows/wf_f5814c54-d50/agent-a42ad719c4825bd92.jsonl
  session_started: '2026-06-22T12:17:52.192Z'
  session_ended: '2026-06-22T12:27:02.335Z'
  session_size_bytes: 214283
  truncated: false
---

## Decisions made

The session established that the recommended architecture for the single ReadyStack argocd release is Path 2: k3s server run with `--rootless --disable-agent` as image USER 999, with supervisord and KUBECONFIG relocated under `$HOME`. This was not a final ship decision but a confirmed architectural direction backed by empirical reproduction.

The session also decided that "Docker Scout Grade A" in buy.html marketing requires all eight default Scout policies to be green, including the Default-non-root-user policy — not merely 0 fixable Critical/High. This interpretation was treated as binding, citing PRODUCT_VISION_ALIGNMENT and SCOUT_SCORING.md.

The session decided the release must be a single SKU (one tag: `readystack/argocd:v3.4.3-CE-ubuntu24.04-r1`), functioning as both the free DockerHub image and the basis for the paid Agent-ready Archive. A two-SKU split or a non-graded demo was explicitly ruled out.

The adversarial reviewer decided not to uphold the claim that the recommended single release currently matches every buy.html bullet, citing five bullets that are literally false on existing artifacts and one CVE claim that is unproven on the required base OS.

## Open questions

Whether to rebase to ubuntu:24.04 (recommended, matches the existing tag, LTS) versus accepting ubuntu:25.10 and re-tagging — owner confirmation required. The discovery rebuild on 24.04 has not been confirmed CVE-clean on a fat image.

Whether `k3s --rootless` experimental-flag is acceptable, given that the experimental userns/network path is never exercised with `--disable-agent`. Path 1 (non-root API-only k3s with hand-rolled writable paths) is the alternative if zero experimental flags are required.

Which key or identity signs the SBOM and provenance attestations (keyless OIDC vs. a managed key). cosign is not installed on prep1 and no signing key is identified.

Whether there are any Scout policies intended to ship with a documented exception. The argocd image is Apache-2.0 so policy #4 (No AGPL) is expected to be naturally green — owner should confirm the all-8-green gate enforces an empty exception allowlist.

Whether CVE=0 is preserved after rebasing the discovery and fat images to ubuntu:24.04. This has not been scanned.

## People mentioned

No individual people were named in this session.

## Dates and deadlines referenced

No specific dates or deadlines were referenced in this session. The session date context is 2026-06-22 (session start).

## Thoughts not yet in the canvas

The session identified that `strategies/argocd.json` contains an explicit false assertion (noted at line 341) claiming that non-root is "structurally unfixable while k3s is embedded — needs root+--privileged." The empirical rootless test disproved this claim. This is a concrete data-quality issue in the strategy file that should be corrected as part of closing the `gate_scope_open_decision` item.

The verdict gate in `registry/stages.json` (seq 70) is currently `wired: false` and checks only whether the Scout verdict JSON parses — a Grade-C result passes the gate today. No `docker_scout_grade_a` claim object exists in `registry/claims.json`; only a `vulnerability_scan` claim covers the 0-fixable-C/H sub-bullet. These encoding gaps mean the pipeline does not enforce the Grade-A marketing claim at any automated checkpoint.

The `runtime_user` field in the argocd strategy is described as decorative rather than load-bearing — the fat image ships as root while the strategy declares `runtime_user=argocd`, a contradiction the pipeline does not catch.

The offline vendoring gap was empirically confirmed: running `docker build --network=none` against the current kit exits at the first apt-get call (exit 100). The source Dockerfile also performs `git clone`, `go mod download`, and `yarn install` at build time. No `Dockerfile.offline`, `BUILD_OFFLINE.sh`, `VERIFY_OFFLINE.sh`, or vendored apt/source directories exist in the argocd kit, unlike the gitea-kit which does have these artifacts.

The rc4 image SBOM attestation is an empty object. cosign is not installed on the build host. Both of these block the "SBOM included" and "signed provenance" free-chip bullets as well as Scout supply-chain attestation policy #5.

The nine-step concrete plan the session produced was treated as the path forward, but as of session end none of the steps had been executed.

## Status changes implied

No explicit milestone slug transitions were stated. The session implicitly moved the argocd non-root work from an open/unresolved state to a confirmed architectural path (Path 2 rootless), but no canvas milestone slug was referenced to formalize this.

