---
schema_version: 1
audit_id: 2026-05-20T16-09-03-gadzoom
product: gadzoom
generated_by: claude-sonnet-4-6
generated_at: '2026-05-20T16:08:00Z'
sources_summarized:
- kind: claude_session
  path: /home/readystack/.claude/projects/-home-readystack/18d19cee-fe99-496f-b20d-fa9d3baf4a2b.jsonl
  session_started: '2026-05-07T15:38:01.252Z'
  session_ended: '2026-05-20T16:06:54.399Z'
  session_size_bytes: 8044186
  truncated: true
---

# Session 2026-05-07 — msgschool cost investigation, LLM migration, agent rebuild, and public-repo scrub

## Decisions made

The operator confirmed that the $50 Moonshot/Kimi token burn was attributed to a runaway local OpenClaw gateway session on exceedbot1 under the tombombadil user, not the msgschool VM. The gateway had been running continuously for approximately four days driven by a 30-minute heartbeat loop and a non-empty HEARTBEAT.md file that prevented the built-in skip rule from firing. A Moonshot API key was shared between the exceedbot1 gateway and the msgschool VM, making console attribution ambiguous. The operator killed all Kimi/Moonshot API keys to stop the spend.

Tom's HEARTBEAT.md was edited to a single header line so the OpenClaw built-in skip rule would fire and suppress API calls during idle periods.

The msgschool canvasagents were migrated from openrouter/qwen-3.6 as primary to xai/grok-4.3 as primary, with anthropic/claude-sonnet-4-6 retained as fallback. The xAI provider block and API key were copied from Tom's local gateway configuration. The msgschool openclaw gateway was hot-reloaded to pick up the change.

The operator directed a comprehensive documentation, website, and public-repo audit for msgschool. All seven priority fixes previously identified were applied: the home page line 42 was revised to avoid the word "credentials" in the sense of user-supplied school credentials; the architecture.md credential-handling section was rewritten to match the live code; architecture.json llmModels.rotation was updated to reflect xai/grok-4.3 primary; privacy/page.tsx LLM provider list was updated to drop Moonshot and feature xAI; two security-page overpromises (zero-fill on eviction, offline backup key) were softened; privacy-page line 77 was clarified to distinguish msgschool-account password from Skyward password. Three stale documentation files were deleted from the public repo: ADMIN_PANEL_SPEC.md, PRIVACY_DEFENSIBILITY_WORKPLAN.md, and ROADMAP.md. All changes passed a clean Next.js build. Changes were left uncommitted pending operator direction.

The operator directed that the msgschool agent instruction set be rebuilt lean and toolsd-native. The rebuild was completed: AGENTS.md, TOOLS.md, and IDENTITY.md were rewritten as lean files totaling approximately 18 KB replacing approximately 85 KB of contradictory, partially unmigrated content. PERSONA.md was retired. Symlink injection was replaced with real file copies in all three live workspaces and in provision.sh. Injection was verified with a test agent showing projectContext rising from 2.4 KB to 20 KB.

A verified clean-session test of qwen showed it calling ms_call pulse.combined with the correct tool rather than fabricating from session history. The prior fabrication was caused by a /reset bug where session rotation was a silent no-op, leaving 312 prior entries including completed pulse reports for qwen to copy.

A hard 90-second wait instruction was added to TOOLS.md for slow ms_call commands, with no polling fallback. This was deployed to all three live workspaces.

A deterministic Canvas-to-Skyward course reconciliation module (reconcile.ts) was written and deployed into toolsd. The toolsd daemon was restarted with the new code.

The Skyward portal at student.canyonsdistrict.org was found to be down at the district level for all callers, not only the msgschool VM. The root cause identified is the Canyons district migration from Skyward SMS (wsisa.dll) to Skyward Qmlativ for the 2026-2027 school year. The old application is no longer responding. When Qmlativ becomes accessible, skyward.ts in toolsd will require a complete rewrite.

The surfacemarket subdirectory was extracted from the gadzoom monorepo into its own standalone repository at c0inz/surfacemarket. All five files were committed and pushed to the new repo. The surfacemarket/ directory was removed from gadzoom and the deletion was pushed. Git author identity was set to John Davenport / johntdavenport@exceed.io for both repos.

A CONNECT-BOMBADIL-TO-LOCAL-LLM.md runbook was moved from the .spyglass/uploads hidden directory to ~/projects/gadzoom/docs/ with a clean filename.

A new ROADMAP.md was created at ~/projects/msgschool/docs/ROADMAP.md with seven open items grouped as Skyward, toolsd and agent, platform, and housekeeping tracks. All prior session tasks were cleared.

The operator directed that the msgschool public repo be purged of internal infrastructure information and that the originals be saved to a private location. The agent was preparing to create a private repo (c0inz/msgschool-ops) to hold the un-scrubbed originals before scrubbing the public repo. This work was in progress at session end.

## Open questions

Where should the un-scrubbed internal ops documentation be saved before the public repo is purged? The agent asked for a save-location choice and was executing a private-repo creation at session end; it is not confirmed completed.

Whether the CONNECT-OPENCLAW-TO-LOCAL-LLM.md runbook in .spyglass/uploads/ should be moved to docs/ or deleted.

Whether the second placeholder entry in the UNLIMITED_TELEGRAM_USER_IDS list (representing a user named Sarah) has a real Telegram ID to substitute.

Whether to add an env-var override mechanism so the real unlimited-user list lives in /etc/msgschool/.env and the public source stays placeholder-clean, preventing the next deploy from overwriting prod's real values.

Whether to expand toolsd with three additional Canvas data fields (assignment created_at, syllabus_body, calendar events endpoint) to back the currently unsupported /events, /syllabusdrift, /makeuprules, and schedule-drift section of /pulse.

Whether to implement a deterministic pulse.report toolsd tool that computes all drift patterns in TypeScript, removing the LLM from that data-assembly path entirely.

Whether to proceed with a per-user OS-level UID separation for the toolsd daemon (task #27), which is blocked on OpenClaw's single-gateway-process architecture.

Whether to fix the /reset silent-no-op bug (task #29) at the gateway level.

Whether the msgschool docs changes, the ROADMAP.md, and the new api/ backend for gadzoom should be committed and pushed.

Whether the gadzoom api/ directory and PHASE0_LOG.md, which represent approximately one week of Phase 0 partner-grade rebuild work and exist only on the local VM, should be committed and pushed to prevent loss.

## People mentioned

John (operator, also referenced as JohnTDavenport, TG id 679381326).
Tona (real msgschool user, TG id 718364807).
FuzzMgr (John, TG id 8421020953, real msgschool user, inactive since 2026-05-01).
X (unidentified user, TG id 5595076930, bounced after one message without redeeming access code).
Sarah (referenced as a second placeholder in the unlimited-user list; real Telegram ID unknown).
Weston Daventon (student whose pulse report was tested; name appears in agent output).
Tom (tombombadil gateway owner on exceedbot1).

## Dates and deadlines referenced

2026-04-20: Probe test row last seen in ms_users.
2026-04-30: Observed credential leak event on a test account that motivated the toolsd design.
2026-05-01: FuzzMgr last message; toolsd migration completed (canvasagent scraping moved to daemon).
2026-05-03: Public commit of msgschool repo (initial public commit, 2bd0573); surfacemarket content present in gadzoom.
2026-05-04: Start of the runaway tombombadil gateway session (12:22 UTC); PM2 daemon on the msgschool VM had been dead since this date at 00:32.
2026-05-05 (approximate): Kimi/Moonshot token burn began approximately 36-48 hours after the repo went public.
2026-05-07: Session date; Tona had a single brief conversation this morning.
2026-05-09: Last confirmed successful Skyward toolsd call per audit table.
2026-05-15: Last confirmed successful Skyward toolsd call before the district portal went dark; also the date of a prior Kimi-run pulse report that qwen was copying from.
2026-05-17 21:19 UTC: Clean-session verified pulse test; qwen called ms_call pulse.combined with live data.
2026-2027 school year: Canyons district Qmlativ migration window, reason for Skyward SMS portal going dark.

## Thoughts not yet in the canvas

A second OpenClaw install was discovered at /home/readystack/.openclaw/ on exceedbot1 with a dormant PM2 process that had been dead since 2026-05-04. This install exists on the same host as the runaway tombombadil gateway but was not the source of the spend.

A johntdavenport@exceedbot1 systemd user unit for openclaw-gateway.service is enabled but inactive. A reboot could reactivate it. Disabling it was recommended but not confirmed done.

The OpenClaw heartbeat's built-in skip logic (isHeartbeatContentEffectivelyEmpty) fires only when HEARTBEAT.md contains exclusively whitespace, markdown headers, and empty list stubs. Any plain text, including the phrase "No active tasks.", defeats the skip and causes a live API call every tick. This is a non-obvious operational footgun documented here for the first time in session context.

The ewh (ExceedWebsites) host was found to have an active OpenClaw gateway at root with a 3d 12h uptime performing CleanRelay queue work at approximately $0.30-0.40/day on Anthropic Sonnet. The HEARTBEAT.md content on ewh describes "no polling on regular heartbeats — only when woken by CleanRelay," but the gateway still wakes the agent hourly regardless. It was flagged as medium-burn and warranting investigation of whether a separate event mechanism exists for CleanRelay traffic, but no changes were made.

The super-server (192.168.1.153), crd, prep3 (192.168.122.189), prep4 (192.168.122.137), and mombadil-hosted VMs were unreachable during the fleet audit. Their OpenClaw status is unknown.

Nine systemic concerns about OpenClaw heartbeat cost were enumerated: non-empty HEARTBEAT.md as a silent always-on burn trigger; fallback chains amplifying cost on turn failures; long-running sessions accumulating context indefinitely; absent per-call cost telemetry in msgschool's agent_usage table; same API keys shared across multiple gateway configs; heavy bootstrap context loaded per heartbeat tick; heartbeats running 24/7 with no activeHours restriction; stale enabled gateway service units that survive reboots; and audit blind spots on unreachable hosts.

The /reset command in msgschool is a silent no-op. It rotates the state/session-nonce file but the OpenClaw gateway ignores the rotated session ID and continues appending to the agent's original session file. Every prior /reset in the product's history has had no effect.

The msgschool build surface a pre-existing warning: "next start does not work with output: standalone configuration; use node .next/standalone/server.js instead." The server runs despite the warning but the systemd unit could be made faster by switching the invocation. Not changed.

The gadzoom api/ directory contains the entire Phase 0 backend (Fastify, Prisma, Postgres migrations, EIP-712 and HMAC auth) and has never been committed. PHASE0_LOG.md documents approximately one week of completed Phase 0 work. Both are local-only and would be lost if the VM were destroyed.

The msgschool public repo contains a hardcoded dev-fallback database connection string with a weak credential as localhost-scoped code. The format matches a database connection string pattern and should be replaced with an obviously-non-real placeholder even though it is localhost-scoped.

A second runbook file (CONNECT-OPENCLAW-TO-LOCAL-LLM.md) remains in .spyglass/uploads/ on the gadzoom workspace; disposition was not decided.

The Canyons district Qmlativ migration will require a complete rewrite of toolsd's skyward.ts. The existing implementation targets the old Skyward SMS application (wsisa.dll, login popup via #login/cbs("bLogin"), the a[id="showGradeInfo"] DOM selector). Qmlativ is a different web application. The rewrite cannot begin until the new portal is accessible.

The toolsd per-UID structural isolation gap (task #27): the v1 implementation runs as root in the same UID as the rest of the platform. The structural guarantee that the agent process physically cannot read credential files is not yet enforced. Behavioral redirection (no instruction path to credentials) is in place; OS-level enforcement is deferred.

The deterministic pulse.report toolsd tool was proposed but not scoped or implemented. The current toolsd returns structured JSON facts; all drift computation and report prose assembly is still done by the LLM. Moving that logic into TypeScript was agreed in principle but left as an open task.

## Status changes implied

No explicit track or milestone slugs were present in the session canvas snapshot (§7 was empty). No slug-based status changes can be recorded. The following work-state changes are implied by the session and are captured here for triage:

msgschool LLM provider migration from openrouter/qwen to xai/grok-4.3: completed and live in production.

msgschool agent instruction-set rebuild: completed and deployed to all three live workspaces and to provision.sh.

msgschool heartbeat burn (tombombadil exceedbot1 gateway): mitigated by HEARTBEAT.md edit; Kimi keys killed; gateway still running on other providers.

msgschool toolsd reconcile.ts join: code deployed and daemon restarted; matching logic unverified against real Skyward data due to district outage.

Skyward SMS portal (student.canyonsdistrict.org): down at district level, blocking all Skyward data for all msgschool users; contact Canyons IT recommended.

surfacemarket extraction from gadzoom monorepo: completed and pushed to c0inz/surfacemarket; deletion pushed to c0inz/gadzoom.

msgschool public repo scrub of internal infrastructure information: in progress at session end; un-scrubbed originals save step was being executed.

msgschool ROADMAP.md: created and saved locally, not yet committed.

msgschool website and documentation audit: all seven priority items applied locally, build passing, not yet committed or deployed to prod.

