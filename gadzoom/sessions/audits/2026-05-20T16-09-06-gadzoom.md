---
schema_version: 1
audit_id: 2026-05-20T16-09-06-gadzoom
product: gadzoom
generated_by: claude-sonnet-4-6
generated_at: '2026-05-20T16:08:00Z'
sources_summarized:
- kind: claude_session
  path: /home/readystack/.claude/projects/-home-readystack/4e8716ab-cb9f-4e37-b85b-1b2f0af63527.jsonl
  session_started: '2026-05-08T01:02:39.556Z'
  session_ended: '2026-05-14T16:03:54.056Z'
  session_size_bytes: 3054991
  truncated: false
---

# Session 2026-05-08 to 2026-05-14 — GADZ repo cleanup, surfacemarket.ai build, Phase 0 API implementation

## Decisions made

The `agentic-site-spec` branch (39 files, 5 commits ahead of main) was merged into main using a `--theirs` strategy for all conflicted files, since that branch had 30+ commits of refinement versus main's single-shot import. The branch was then deleted from GitHub.

Dead code directories (`oracle/`, `monitor/`, `warden/`, `contracts/`, `illustrator/`, root `wrapper/`, `sdk/`) and all concept-only documents (token docs, storyboards, old specs, old task files) were moved to an `archive/` directory and pushed.

`ArchiveRisk.md` was created with 7 sections and 43 items documenting every unresolved design question, assumption, strategic gap, legal risk, and contradiction found in the archived material.

surfacemarket.ai was chosen as the first site to build, before adpix.ai, because its surface cards are uniform rectangles that avoid the layout complexity of the favicon-grid.

The UI aesthetic for surfacemarket.ai was changed from dark crypto to warm marketplace (cream background, white cards, orange CTAs, amber pricing) after the initial dark design was rejected.

The call-to-action button label was changed from "Own This" to "Resurface" across all card and modal copy. Language referring to "buying a site" was removed; the platform vocabulary is "surface" throughout.

The affiliate store ID `fablesoftware-20` was set as the Amazon tag for all affiliate link surfaces.

A second surface type, bio link (profile image + first 25 characters of description + URL), was added to the surfacemarket spec alongside affiliate links.

surfacemarket.ai was deployed to Cloudflare Pages using a Cloudflare account-owned API token with Pages:Edit scope. Custom domains surfacemarket.ai and www.surfacemarket.ai were wired via CNAME and registered as Pages custom domains. The Cloudflare account ID is `e01b586694f7ebd57668fa609be114f4`. The account-owned token used for deployment was stored in `~/credentials/cloudflare.json` as `pages_token`; the value is redacted here.

The test-phase auth wall was switched from `crypto.subtle` SHA-256 (which had browser-compatibility issues on Windows) to a `btoa`-based scheme. The access code hash used in production was set by the user; the hash value and original passphrase are redacted. The passphrase was inadvertently echoed in plain text during the session — a feedback rule was saved to never repeat credential values verbatim.

15 milestones were written across 4 streams: gadz.ai economic engine (M1–M3), surfacemarket.ai (M4–M8), adpix.ai (M9–M11), GADZ token (M12–M15). M4 (shell deployed to Cloudflare Pages) was completed during the session.

Polymarket was set as the architectural floor for the entire platform. The decision was made to rebuild the backend to Polymarket-comparable sophistication before adding further features.

`ARCHITECTURE.md` (530 lines) and `POLYMARKET_RESEARCH.md` (317 lines) were written documenting the target production architecture and the research behind the architectural choices.

Phase 0 was started: a separate `gadzoom` database and role were created on ExceedBackendDB (164.92.79.17, PostgreSQL 16.13). The gadzoom database is isolated from co-tenants `aiglass_prod` and `usaprimecuts` via separate roles and pg_hba lines. DB credentials were stored at `~/credentials/gadzoom-db.json` (chmod 600); the password value is redacted here and was flagged for rotation as it appeared in the session transcript.

Prisma was pinned to version 6 because Prisma 7 shipped a breaking config change during the session. A shadow database (`gadzoom_shadow`) was pre-created to avoid granting CREATEDB to the gadzoom role.

The Prisma schema uses `onDelete: Restrict` on SurfaceEvent and Invoice relations, treating surface events as an immutable audit log that cannot be orphaned.

The HMAC secret column was changed from a bcrypt hash to plaintext storage (renamed `secret_hash` → `secret`) because HMAC verification requires the key bytes, not a hash of them. An AES-GCM at-rest encryption step was added to the Phase 1 punch list.

The Harberger auto-acquire rule was implemented: when a lease is activated, the surface's declared value is set to the invoice amount, so the next buyout floor moves with it.

JSON state from `banner-market/data/*.json` was migrated to the Postgres schema. Two surfaces and two history events were migrated. Four analytics fields (`balance`, `pageViews24h`, `clickRate`, `clickDeltaPct`) and four single-valued fields were intentionally dropped with documentation.

A Fastify 5 + TypeScript + Zod API was built in `~/projects/gadzoom/api/` with 10 versioned endpoints, envelope-shaped responses, request IDs, structured pino logging, and Helmet + CORS. EIP-712 (L1 wallet signature) + HMAC-SHA256 (L2 per-request) dual-tier auth was implemented and smoke-tested end-to-end. Read endpoints remain intentionally public.

`PHASE0_LOG.md` was created with the original Phase 0 plan, retroactive estimates, a 10-item decisions journal (D-1 through D-10), and the full prioritized punch list.

CI/CD (GitHub Actions) was explicitly parked by the user for a later session.

## Open questions

Whether "agent epc" uses ExceedBackendDB remains unanswered. The investigation found no epc-related database, user, or service on the host, but the sadb canonical server registry was not successfully queried before the question was interrupted.

Which chain will GADZ token launch on — Litecoin EVM (Flare/FAssets or native if testnet is ready), Base, or Stellar — is unresolved. This is the longest-pole item blocking token milestones M12–M15.

Whether Litecoin's smart contract testnet is production-ready enough for v1 is unresolved. The user stated it exists and the plan is to dig into it; v2 fallback was acknowledged.

Five open items in `gadz-token/TOKEN_LEGAL_STRATEGY.md` require a crypto-securities attorney call before the token can go into a UI.

Whether the surfacemarket auth wall warm-aesthetic reskin has been applied is unresolved; the user confirmed the content loaded but the sign-on screen still showed the old dark styling at session end.

The access code for surfacemarket.ai should be rotated; the original passphrase appeared in the session transcript.

The gadzoom DB password should be rotated; it appeared in the session transcript.

## People mentioned

John (user/owner, johntdavenport@exceed.io).

Tombombadil (devops agent, ran on exceedbot1, added SSH pubkey to ExceedBackendDB and reported droplet status).

## Dates and deadlines referenced

2026-03-17: expiry date of the `gadz-ai-top-banner` lease on gadz.ai. The surface has been in an expired/available state for approximately 8 weeks as of the session.

2026-04-27: date the SSH config aliases (ewh, crd, mp, prodasset) were last present in tombombadil's `~/.ssh/config`.

2026-05-10: approximate date Prisma migrations were applied and the gadzoom database was created on ExceedBackendDB.

"Two months" was the planning horizon the user cited when evaluating whether to commit to the GADZ project, and again when specifying the 1-million-user target for architecture planning.

## Thoughts not yet in the canvas

The user described adpix.ai as a Harberger-tax favicon-grid variant on the Million Dollar Homepage. Each cell is the smallest unit allowing brand recognition. Contiguous multi-cell owners get their image overlaid across owned cells, cropping at boundaries of unowned cells. No milestone or slug exists for this in the current canvas.

The user described surfacemarket.ai's second content type as a "profile top bio link" — a surface card showing a profile image and the first 25 characters of a description plus a URL. This is captured in the SPEC.md but has no milestone slug.

The user proposed a "last 10 buyers get a piece of the sales commission" mechanic — a rolling early-adopter rebate window built into every exchange. Legal analysis in `TOKEN_LEGAL_STRATEGY.md` concludes this is acceptable as an early-adopter incentive rather than a recruitment incentive, with the recommendation to keep payouts in GADZ and cap at approximately 3%.

The user proposed that up to 50% of protocol revenue be purchasable via the same Harberger tax mechanism. Legal analysis flagged this framing as potentially Howey-test-dangerous as a revenue share; the recommended replacement is a buy-and-burn treasury model (protocol buys GADZ weekly and burns it, no holder ever receives a payment). No milestone slug exists for the treasury model.

The user proposed that GADZ token holders pay a lower tax rate on surface transactions, while non-holders have the equivalent GADZ acquisition cost included atomically in the transaction (auto-acquire mechanic). The agent assessed this as the strongest token-adoption mechanism in the design. No milestone slug exists for this mechanic.

The user specified multi-wallet support: MetaMask plus two other wallets (likely Coinbase Wallet and WalletConnect). No milestone slug exists for wallet integration.

Payment clearing in USD, Stellar, Litecoin, and Dogecoin was specified. No milestone slug exists for multi-currency clearing.

The `eip712.ts` default chain ID was set to 137 (Polygon) as a placeholder. The final chain for L1 auth has not been decided and may change when the GADZ token chain decision is made.

ExceedBackendDB is confirmed to also run nginx (ports 80/443), MariaDB, and a Node.js process on port 3000, making it a mixed-purpose host rather than a dedicated database server. The 1 vCPU / 2 GB sizing was flagged as insufficient for production load. A resize-before-launch note exists in PHASE0_LOG.md but no milestone tracks this.

Two dev-only firewall rules (DO cloud firewall and ufw on ExceedBackendDB) opening port 5432 to the current VM's egress IP were added during the session and must be removed before any public exposure. These are not tracked in any milestone.

The `banner-market/server.js` process on ExceedWebsites (146.190.117.93) is still running and serving gadz.ai. A cutover plan to deprecate it in favor of the new Fastify API is in the Phase 1 punch list but has no dedicated milestone.

PgBouncer connection pooling in front of Postgres is on the punch list as a prerequisite for the 1-million-user scale target but has no milestone.

## Status changes implied

No formal milestone slugs exist in the canvas snapshot (§7 is empty), so slug-format status changes cannot be produced. Based on session content:

M4 (surfacemarket.ai shell deployed to Cloudflare Pages): moved from planned to completed during this session.

Phase 0 API foundation: moved from not-started to substantially complete — PostgreSQL provisioned, Prisma schema migrated, JSON data ported, Fastify API built, EIP-712 + HMAC auth implemented and smoke-tested. CI/CD remains open.

gadz.ai economic engine (M1–M3 equivalent): still blocked. The carry enforcement worker, buyout waterfall, and declared-value / renew endpoints are all on the Phase 1 punch list and were not started.

