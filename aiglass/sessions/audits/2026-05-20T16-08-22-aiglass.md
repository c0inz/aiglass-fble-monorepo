---
schema_version: 1
audit_id: 2026-05-20T16-08-22-aiglass
product: aiglass
generated_by: claude-sonnet-4-6
generated_at: '2026-05-20T16:08:00Z'
sources_summarized:
- kind: claude_session
  path: /home/readystack/.claude/projects/-home-readystack/101f8615-fef4-4a5b-bb67-e020cc0268a7.jsonl
  session_started: '2026-05-06T18:12:35.596Z'
  session_ended: '2026-05-08T01:03:46.701Z'
  session_size_bytes: 3109807
  truncated: true
---

# Session 2026-05-06 to 2026-05-08 — Plan 4a design and foundation build

## Decisions made

The operator (John) confirmed `fble` as the account slug for `johntdavenport@gmail.com`. This slug will prefix all workspace repos using the pattern `c0inz/aiglass-fble-<workspace-slug>`.

GitHub auth for Plan 4 will use the existing classic PAT via `octokitWithPat()` as the single doorway, dropping the originally planned GitHub App for v1. A migration path to a real App is documented in the playbook.

The data model hierarchy was locked as: Track → Milestone → Sub-milestone (max 1 level) → Step → Substep (max 1 level). No milestone nesting beyond one level; no sub-substeps. Steps use done/not-done only.

Dependencies can link any node type (milestone, sub-milestone, step, substep) to any other node type across any track, with a cycle check enforced at parse time.

The vocabulary was locked as plain universal terms: Track, Milestone, Step, Substep, Dependency. "Change request" (CR) is reserved for aiglass-originated suggestions only (capability gaps, drift triage moves). Session-ingested changes land eagerly without a CR.

The repo is the single source of truth. The DB is a read-cache projection of repo YAML. No UI write paths exist except the drift triage CR (a mechanical move-to-track commit).

A two-stage pipeline was designed and locked. Stage 1 (hourly cron, operator's local LLM) produces audit markdown files in `.aiglass/sessions/audits/`. Stage 2 (operator's local LLM, not aiglass) consumes unconsumed audits and writes YAML deltas plus receipt files in `.aiglass/sessions/receipts/`. Both stages run on the operator's side; aiglass does no LLM calls in the audit path.

Audit consumption cadence for stage 2 was locked as daily cron. The operator also has a "Process now" override button in the UI.

After the operator clarified that both LLM stages run locally at no cost, the aiglass-side `auditProcessor.ts`, `audit_processing_runs` table, cost-cap logic, and `llmChat` calls were removed from the spec.

The orphan/drift design was locked: a system-protected `Drift` track (8th default track, ordinal 0) absorbs any delta that references an unknown track, unknown parent milestone, or unknown dependency ref. Creation-shaped ops auto-stub; state-change ops on unknown targets remain strict and reject. Moving a milestone out of Drift requires a CR opened from the Drift triage UI.

Session file deltas are committed eagerly to main. A session ingest row is idempotent on `(workspace_id, file_path, file_sha)`. The receipt-file-existence in the repo is the consumed marker; no DB flag is used.

The spec addendum `docs/superpowers/specs/2026-05-06-aiglass-plan-4-sessions-and-milestones.md` was written and committed across multiple revisions (commits `f82fb20`, `e6c67d1`, `70da329`). Final length: 1161 lines.

A self-contained stage-1 audit cull harness contract was written to `docs/playbooks/audit-cull-harness-v1.md` (commit `d1ab24c`, 398 lines). It is generic and parameterized; no workspace-specific values are hardcoded.

Plan 4 was split into Plan 4a (audit consumption MVP, 24 tasks) and Plan 4b (products and capability gaps, outline). Both were committed at `226a0a7`.

Plan 4a Tasks 0 through 7 were executed via subagent-driven development and committed before the session ended. The foundation layer (schema migration, env additions, octokitWithPat doorway, JSON schemas, parsers, ref grammar, and applyDeltas) is complete. 195/195 tests were passing at session end.

`SESSION_STATUS.md` was updated and `RESUME_PLAN_4A.md` was written as a seed prompt for the next session.

## Open questions

The spec lists six open items at §17 (later §16/§17 after restructuring), including: track tab placement in the UI, schema versioning strategy for YAML files, audit token budget defaults, whether default tracks are user-editable, the scope of drift triage (can users triage from the Tracks tab or only the Drift section), and stub naming visibility (whether auto-stub slugs surface in the UI with a visual indicator).

The stage-2 audit-process harness contract (`audit-process-harness-v1.md`) was acknowledged as forthcoming but not yet written.

Whether aiglass should bundle a reference Python implementation of the stage-2 harness in `playbooks/local-stage2-harness/` was flagged as a question but not resolved.

The operator's local LLM setup was noted as approximately one week away from being ready at the time of the session.

## People mentioned

John (operator, owner of `johntdavenport@gmail.com`, account slug `fble`). Sarah and Mike appear as example names in the UI mockup only; they are not real project participants referenced in decisions.

## Dates and deadlines referenced

2026-05-05: original Plan 4 spec, plan, and playbook committed (commits `c0cdcd8`, `c0ad831`, `d614c46`).

2026-05-06: spec addendum written and first committed.

2026-05-07: Plan 4a and 4b plan documents written and committed. Operator's local LLM was noted as approximately one week away.

2026-05-08: session end date per blob metadata. Tasks 0–7 complete at session close.

Daily 04:00 UTC: the default cron slot planned for stage-2 processing runs.

## Thoughts not yet in the canvas

The operator created the first workspace repo `aiglass-fble-sitepilot` manually on GitHub and added a `/sessions` directory to it before any aiglass tooling was built. This was a hands-on proof-of-concept ahead of the provisioning task.

The operator explicitly does not want the canvas vocabulary to feel like Jira or legacy PM tooling. The design intent is a non-developer, marketing-centered creative thinking canvas where terminology should be universally recognizable but not enterprise-coded.

The question of a canonical top-level product planning document was raised. The answer converged on the auto-generated `session-audit-playbook.md` (rendered by aiglass from YAML state) serving as the effective canonical plan artifact, since it is what the local LLM agent reads each run. No separate `plan.yml` source-of-truth file was added.

The operator noted he may not consume audit records on the same interval they are produced, which drove the unconsumed-tracking design (receipt-file-existence as the marker).

## Status changes implied

No pre-existing canvas slugs are present in the playbook §7 canvas snapshot; the canvas is empty at this stage. The following status changes are implied from the session activity:

plan-4a/task-0: pending → complete (pre-flight verified, design refinements folded into spec).
plan-4a/task-1: pending → complete (Drizzle migration, 10 new tables, CHECK constraints, 79 tests).
plan-4a/task-2: pending → complete (env additions, `.env` updated).
plan-4a/task-3: pending → complete (`octokitWithPat` doorway, ESLint guard).
plan-4a/task-4: pending → complete (5 JSON schemas authored and validated).
plan-4a/task-5: pending → complete (parsers for tracks, milestones, audits, receipts).
plan-4a/task-6: pending → complete (ref grammar module).
plan-4a/task-7: pending → complete (`applyDeltas` keystone module, 195 tests passing).
plan-4a/tasks-8-through-23: remain pending at session end.

