---
schema_version: 1
audit_id: 2026-05-20T16-10-19-usaprimecuts
product: usaprimecuts
generated_by: claude-sonnet-4-6
generated_at: '2026-05-20T16:08:00Z'
sources_summarized:
- kind: claude_session
  path: /home/readystack/.claude/projects/-home-readystack-projects-usaprimecuts/ab2f2f7c-b02d-49e9-ae70-5d3f37e82aeb.jsonl
  session_started: '2026-05-04T02:52:15.973Z'
  session_ended: '2026-05-04T23:39:47.076Z'
  session_size_bytes: 977704
  truncated: false
---

# Session 2026-05-04 — Melaleuca compliance review and Riverbend Ranch rerank

## Decisions made

The operator decided to keep Riverbend Ranch on the usaprimecuts.com site as an editorial listing with no affiliate link, no referral code, and no call-to-action. The only link to Riverbend Ranch will point to their main public site.

The operator decided to use Riverbend Ranch's own public claim ("certified 100% USDA CHOICE" per their FAQ) as the grade basis, setting `choice = 1` for the Riverbend Ranch database row, after the assistant found that Riverbend's marketing metadata and FAQ both assert USDA Choice. The earlier instruction to set the brand as Select was superseded by this rule: ungraded if they do not say, matching their claim if they do.

The operator decided to add a `select` column to the `companies` schema for future use even though no brand is assigned Select at this time.

The operator confirmed that `grass_fed` is an attribute, not a grade, and agreed it should be removed from the grade-tier resolution chain in `lib/db.ts`.

The operator decided to use the canonical stripped product URL `https://www.riverbendranch.com/products/riverbend-rancher` (without subscription or variant parameters) after the assistant confirmed it resolves to a 200 response.

The operator decided to replace the existing "Ranch Hand" bundle entry for Riverbend Ranch with the Riverbend Rancher product ($195, 3.75 lb, 4 cuts: Ribeye, New York Strip, Sirloin Steaks, Ground Beef).

All changes were committed and deployed to the live server. The deploy was verified with HTTP 200 responses on the home page, companies page, Riverbend Ranch slug page, and best-bundles page.

## Open questions

The grade tier for grass-fed-only brands without a USDA quality grade (e.g., Force of Nature, Wild Pastures) was not explicitly resolved. The assistant proposed either keeping a "Grass-Fed" display fallback or letting those brands read "Ungraded." The operator did not give a definitive answer before the session ended.

The operator asked about switching to a light theme and moving bundles to the front page with rankings on a secondary page. This was raised early in the session but never addressed after the Melaleuca compliance discussion took over.

## People mentioned

Tona (referred to as the active Melaleuca Marketing Executive whose ME account and personal referral code are involved in Riverbend Ranch enrollment; no last name given in session).

## Dates and deadlines referenced

2026-05-04: the session date on which all database migrations and the deploy were executed. No future deadlines were set.

## Thoughts not yet in the canvas

The operator explored several potential monetization models involving Melaleuca that the assistant assessed as non-compliant or high-risk: a gated "referral buyers only" lead-capture funnel with AI callback, buying search keywords for Melaleuca brand terms, and a "contact me for recommendations" CTA on reviews that would funnel into a Melaleuca Overview call. All three were ultimately rejected by the operator after the assistant's analysis. No milestone exists for any Melaleuca-related funnel work.

The operator expressed frustration with Melaleuca's restrictive policies and the assistant framed the tradeoff between keeping ME status versus freely building out usaprimecuts.com. The operator did not make a decision on whether to retain or drop ME status.

The assistant identified a Riverbend Ranch `prime_score` data integrity problem: the stored value was 10.00 (which the assistant called broken or stale) and `score_value` was 12.70 (over cap). The assistant corrected `prime_score` to 5.5 and `score_value` to 2.0 based on $52/lb pricing for a Choice-grade product. No canvas milestone covers data auditing or scoring corrections.

The operator mentioned the possibility of Tona standing up a separate Approved Provider site (Responsive Data or SX3) under her ME account for private Melaleuca outreach, off usaprimecuts.com entirely. No decision was made and no milestone exists for this.

A database connection string and database credentials were used by the assistant during this session to apply migrations via the `exceedwebsites` host. Values redacted per §3.1.

A GitHub personal access token was retrieved from a credentials file on the local VM to authenticate the git pull on the deployment host. Value redacted per §3.1.

## Status changes implied

No named track or milestone slugs are present in the canvas snapshot (§7 is empty). The following changes are implied but cannot be mapped to existing slugs:

Riverbend Ranch database row: grade corrected from no-flag (broken prime_score=10) to `choice=1` with `prime_score=5.5` and `score_value=2.0`; bundle replaced; affiliate_url cleared.

Schema: `select` column added to `companies` table and to `db/schema.sql`.

`lib/db.ts`: `getGradeTier()` updated to include Select tier and remove Grass-Fed grade fallback; `Company` interface updated accordingly.

Commit `426ec06` pushed to `origin/main` and deployed to `exceedwebsites:/var/www/usaprimecuts.com`.

