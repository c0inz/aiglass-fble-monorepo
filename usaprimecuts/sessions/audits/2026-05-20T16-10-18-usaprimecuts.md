---
schema_version: 1
audit_id: 2026-05-20T16-10-18-usaprimecuts
product: usaprimecuts
generated_by: claude-sonnet-4-6
generated_at: '2026-05-20T16:08:00Z'
sources_summarized:
- kind: claude_session
  path: /home/readystack/.claude/projects/-home-readystack/79422580-502a-4d46-894e-78de594ddf52.jsonl
  session_started: '2026-05-04T02:58:48.373Z'
  session_ended: '2026-05-09T01:25:12.713Z'
  session_size_bytes: 1775488
  truncated: false
---

# Session 2026-05-04 — usaprimecuts editorial affiliate rewrite

## Decisions made

The operator decided to strip the usaprimecuts site down from a feature-heavy community platform to a clean editorial affiliate review site. No user accounts, no user contributions, no community features, and no AI-powered search will be included in the new site.

The Costco/market baseline comparison system was decided to be removed entirely. The operator confirmed they no longer want to promote Costco and disliked the concept of comparing DTC meat prices to grocery store baselines. The replacement approach is peer-based comparison: companies ranked within their own grade tier against other DTC competitors.

The operator decided on a Linear/Vercel-style design system using a dark monochrome palette with a single red accent color (`#E5484D`), Inter font, 8px grid, and 150ms transitions.

The operator subsequently decided to switch to a light theme after seeing the initial dark-themed deployment.

The operator decided that bundles should be the primary content on the homepage, with company rankings demoted to a secondary section. A dedicated `/best-bundles` landing page was also confirmed as needed after initially being reduced to a redirect.

The full rewrite was committed and pushed to `c0inz/usaprimecuts.com` on the `main` branch and deployed to the live site at `https://usaprimecuts.com` on ExceedWebsites (`146.190.117.93`) via PM2 on port 3939 behind nginx with Let's Encrypt SSL.

A `featured_bundles` table was created in the usaprimecuts MariaDB and seeded with 10 curated bundle picks across 7 companies. Bundle contents were subsequently updated to include item quantities (e.g., "4 Ribeye Steaks", "2 lbs Ground Beef").

A GitHub PAT for the `c0inz` account was saved to the agent's credentials file. Value redacted per §3.1 (GitHub PAT).

The MariaDB credentials for the usaprimecuts database were used in session commands. Values redacted per §3.1 (database password and connection credentials).

A DigitalOcean API token was accessed from local credentials to identify the database droplet's public IP. Value redacted per §3.1 (API access token).

## Open questions

Affiliate URLs for all 28 companies in the `companies` table are currently NULL and need to be populated before the site can generate affiliate revenue.

Company logo URLs (`logo_url`) are also NULL for all 28 companies. Images need to be sourced and added.

Deployment connectivity: the site requires a host that can reach the MariaDB at `10.124.0.3:3306` on the private VPC. This is currently satisfied by ExceedWebsites being in the same VPC, but no explicit confirmation was made that this is the intended long-term deployment target.

No decision was made about whether to retain `resend` as a dependency for email capture confirmation emails.

## People mentioned

No individuals were mentioned by name in this session.

## Dates and deadlines referenced

No explicit dates or deadlines were set during this session. The session spanned 2026-05-04 through 2026-05-09 based on session metadata. No launch or milestone dates were discussed.

## Thoughts not yet in the canvas

The operator asked to review a `frontend-design` plugin during the session (installed, then disabled, then re-enabled). The purpose was not stated explicitly in relation to usaprimecuts work, but it was active by the time design work began.

The operator initially had a separate local `master` repo at `~/projects/usaprimecuts` with no remote — unrelated to `c0inz/usaprimecuts.com`. That local repo was discarded and replaced by the GitHub clone. No content from the local repo was preserved or described.

The build on the agent VM could not reach `10.124.0.3:3306` directly, but ExceedWebsites can. The agent noted that any future deployment target will need VPC access to `10.124.0.3` or the DB must expose port 3306 externally. This is a deployment constraint not captured in existing milestones.

The `companies` table in the live DB has no indexes at all, which caused the `featured_bundles` foreign key to be skipped. Adding indexes to the `companies` table was noted but not acted upon.

A new `getCompanyPeers()` DB query function was added to `lib/db.ts` to support peer comparison. This function queries companies within the same grade tier (using boolean flags `grass_fed`, `choice`, `prime`, `wagyu_usa`). This is new logic not present in the original codebase.

The 5-page site structure settled on: `/` (homepage), `/companies` (rankings index), `/companies/[slug]` (company detail), `/best-bundles` (bundle landing page), `/about` (methodology).

## Status changes implied

No existing canvas slugs were referenced in the session. The following changes are implied from session content and are offered for triage:

The site rewrite completed Phases 1 through 3 (gut and scaffold, core pages, SEO metadata) and partially completed Phase 4 (affiliate plumbing structure exists in code but affiliate URLs are not yet populated in the DB). Phase 4 is partially done, not complete.

The site is confirmed live at `https://usaprimecuts.com` with the rewritten codebase as of the end of this session.

