---
schema_version: 1
audit_id: 2026-05-29T18-00-04-gadz-sim
product: gadz-sim
generated_by: claude-sonnet-4-6
generated_at: '2026-05-29T18:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/5f615eed-096d-4187-92e5-a0a6356790f9.jsonl
  session_started: '2026-05-19T04:09:52.387Z'
  session_ended: '2026-05-30T04:33:18.207Z'
  session_size_bytes: 9230845
  truncated: true
---

## Decisions made

The operator decided to create a `~/projects/` directory structure and clone multiple repositories locally. The `gadzoom` repo (`c0inz/gadzoom`) was cloned to `~/projects/gadzoom`. The `surfacemarket` repo (`c0inz/surfacemarket`) was cloned to `~/projects/surfacemarket`. A new empty `adpix` project was initialized in `~/projects/adpix` with a new private GitHub repo created at `c0inz/adpix`. The `c0inz/SoSatisfying` repo was cloned to `~/projects/SoSatisfying`.

A GitHub classic PAT (value redacted) was provided by the operator and saved to `~/credentials/github.json` as `pat_classic_write`, replacing three dead tokens. A fine-grained PAT (value redacted) was also provided but found to have limited repo scope and was superseded by the classic PAT.

The operator decided to build a GADZ surface-market simulation as a new standalone repo (`c0inz/gadz-sim`), scaffolded at `~/projects/gadz-sim`, rather than placing it inside `gadzoom/sim/`. The simulation would use a hybrid agent population: rule-based agents for the majority and LLM-driven agents for a subset to enable observation of emergent reasoning. The LLM model selected for agents is the operator's locally-hosted `qwen3-coder-30b` served via vLLM on LAN host LeagueOfBeasty (192.168.1.108:8000), accessed through an SSH tunnel via bombadil.

The operator decided to reorder the build steps: scale to 100 buyers and traffic shocks first (step 2), then more rule strategies (step 1), then metrics, LLM agents, and dashboard.

The simulation was extended to include: demographic traffic (five demographics: luxury, tech, parents, retirees, gaming), per-demographic CPM rates (based on mid-market 2024–2025 IAB/Statista programmatic display benchmarks net of ~30% exchange take), per-buyer creative vectors, a traditional control-arm kernel (`sim/traditional.py`), and a `compare.py` comparison harness. A `Plugin` buyer archetype was added modeling advertisers who install traditional ad-network plugins (e.g., AdSense) on controlled surfaces, setting a plugin-floor revenue for all surfaces. The operator corrected an assumption: many GADZ buyers are traditional ad service operators whose revenue would be identical to traditional CPM; the operator estimated 70% or more of GADZ buyers would use this model.

The operator confirmed the project cannot use traditional finance rails due to payment reversibility, micro-transaction economics, and the custody-equals-regulation problem. Stellar was selected as the preferred L1 chain (over Solana and Base, which were the assistant's initial suggestions). The escrow contract was dropped in favor of direct atomic on-chain settlement via Stellar's payment ledger, with a non-custodial settlement-service relayer model.

A consumptive mint-and-burn GADZ utility token design was decided upon for protocol tax: stablecoin paid in, token minted and burned atomically per transaction, non-holdable and non-transferable. This design was selected to remove the token-as-security Howey risk vector.

The capital roadmap was decided as a three-instrument structure: (1) consumptive meter token (not a security, not a fundraise), (2) Reg D private placement or Reg CF community raise with a SAFE + token warrant for initial capital, (3) Reg A+ tokenized equity raise at year 2–3 for public ownership. The operator's stated 5-year goal of offering public ownership in the company via tokenized shares was noted as a Reg A+ activity requiring a firewall from the meter token.

The 30-day LLM learning campaign was identified as the major milestone. A milestone report (`docs/MILESTONE-REPORT.md`), a testnet playbook (`docs/TESTNET-PLAYBOOK.md`), and a launch algorithm report (`docs/LAUNCH-ALGORITHM-REPORT.md`) were written and committed to the `feat/demographics-and-revenue` branch of `c0inz/gadz-sim` (PR #1).

The day-one launch algorithm parameters were decided based on simulation data: `tax_rate_per_tick = 3e-4` (~43%/day), `tax_alpha = 0.85`, `lease_window_ticks = 1440`, `guarantee_ticks = 15`, `early_exit_penalty = 0.03`, `transfer_fee = 0.02`, `reserve_frac_range = 0.10–0.30` of TRAD daily yield, and no DV cap on day one (agents self-correct via P&L gradient).

## Open questions

Whether to merge PR #1 or continue iterating on the `feat/demographics-and-revenue` branch was left open at session end.

The late-campaign plateau (mean cumulative P&L peaked day 23 at +$143, then drifted to +$108 by day 29) was noted as warranting investigation; likely cause is strategy notes converging on over-aggressive single-surface concentration.

The residual DV over-declaration (7× fair value at end of 30-day campaign, down from 39×) continues to drift downward but has not converged to 1×; whether further campaigns would fully correct it is unresolved.

Five open items flagged in the launch algorithm report that simulation could not answer: real human DV behavior, actual plugin-arbitrageur composition in a live market, the cause of the late-campaign plateau, owner-veto dynamics, and allocative-efficiency drift under sustained live operation.

The operator-veto semantics (refund vs. blacklist vs. rate-limit) were listed as a decision to lock before any testnet code is written.

Chain selection (Stellar confirmed by operator preference) and custody model (non-custodial embedded wallets confirmed) were noted as resolved; tax denomination on the testnet (fixed 1:1 to test stablecoin vs. floating) was listed as still open.

Which embedded wallet SDK to use (Privy, Turnkey, Web3Auth, Dynamic, or Stellar-native tooling) was not decided; Stellar support among EVM-first SDKs was noted as a verification item.

Whether the `banner-market/` code should seed any product repo beyond `gadzoom` was left open; the assistant concluded it should stay in `gadzoom` as protocol-level reference backend.

## People mentioned

No people other than the operator were mentioned by name in the session.

## Dates and deadlines referenced

The planned testnet competition was described as a one-week duration with no specific calendar date set.

The operator's strategic goal of public ownership offering was described as a 5-year gameplan; no specific dates or milestones were attached.

The assistant referenced mid-market 2024–2025 IAB/Statista programmatic display CPM benchmarks as the basis for per-demographic revenue rates.

The 30-day learning campaign ran 43,200 simulation ticks in 6 hours 4 minutes of wall time, completing before the end of the session.

The `c0inz/gadz-sim` repo HEAD commit at session end was `ae6b4d2` (launch algorithm report, docs commit).

## Thoughts not yet in the canvas

The operator articulated four distinct product descriptions that are not reflected as discrete milestones in any canvas: (1) `gadz.ai` as a protocol marketing site and community for the GADZ protocol, focused on token explanation, Harberger tax, and surface market showcase; (2) `adpix` as a modern version of the million-dollar homepage using usable-sized rectangles and showcasing surface survivability in a real-time Harberger market; (3) `surfacemarket` as the trading platform for surface contracts using the GADZ protocol; (4) `SoSatisfying` as a social media aggregation site where users OAuth/DM-validate control of social accounts, generate an algorithmic aggregate score and cross-posted feed on their own page, with page surface space sold via the GADZ protocol.

The operator indicated that the `gadzoom` monorepo is largely stale and stream-of-thought, with per-product repos now the source of truth. The assistant created a top-level `gadzoom/README.md` flagging this with a staleness warning and per-product table; it is committed locally but its push status relative to the final session state is unclear.

The structural finding that GADZ is a winner-take-all market concentrating value on well-matched whales while locking out roughly half the advertiser pool at default parameters was flagged as a protocol design question — addressable by faster/cheaper force-buys, multi-tenant surfaces, or smarter agent matching — but no decision was made.

The network friction profit concept (what a traditional ad network captures by locking publishers into contracted blended CPMs that don't react to real-time audience demographic shifts) was explicitly modeled in `sim/traditional.py` as a first-class metric; the operator agreed this is the value GADZ captures by being real-time.

The assistant noted the global git credential helper (`~/.local/bin/git-credential-spaiglass`) on the gadz-sim VM is broken and returns dead tokens; a workaround using repo-local credential helpers was implemented but the underlying helper was not repaired.

The operator stated a preference for "open" as the force-buy authorization model (i.e., not requiring surface owner veto approval for force-buys as the default), identifying it as the key Harberger mechanism; an owner veto with rate-limiting per epoch was proposed as a safeguard against veto being used as a grief weapon.

The operator confirmed that the `archive/` directory in `gadzoom` should stay in place rather than be moved to product repos, as moving stale docs would pollute the clean per-product repos.

The `max_tokens=8000` bug in the LLM agent config (a leftover from a prior Kimi reasoning model setting) was identified and fixed to `max_tokens=1400`; this delivered a 7.6× throughput improvement. A prompt prefix-caching refactor delivered an additional 2.6× speedup. Combined, the 30-day campaign was reduced from a projected ~19 hours to ~5.8 hours actual.

The Stellar-native claimable balances and XRP Ledger's native escrow primitive were noted as alternatives to a bespoke smart contract for trust-minimized holds if ever needed at mainnet, without requiring Soroban (Stellar's newer smart contract layer, launched 2024).

The operator expressed that they want the GADZ token to "set the stage" for a future tokenized equity offering and to publish burn-rate statistics publicly; the assistant identified that this sequencing creates securities-law integration and gun-jumping risk, and the operator agreed to use the three-instrument firewall approach instead.

## Status changes implied

No existing canvas track or milestone slugs were referenced. The session represented greenfield project setup and simulation work. No status changes against a pre-existing canvas apply.

