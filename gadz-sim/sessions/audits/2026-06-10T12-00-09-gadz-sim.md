---
schema_version: 1
audit_id: 2026-06-10T12-00-09-gadz-sim
product: gadz-sim
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/5f615eed-096d-4187-92e5-a0a6356790f9.jsonl
  session_started: '2026-05-19T04:09:52.387Z'
  session_ended: '2026-06-04T15:38:12.016Z'
  session_size_bytes: 9392706
  truncated: true
---

## Decisions made

The operator decided to create a new private GitHub repo (`c0inz/gadz-sim`) for the GADZ surface-market simulation rather than placing it inside the `gadzoom` monorepo.

The operator chose a hybrid agent population for the simulation: 95 rule-based agents plus 5 LLM-driven agents (using a local LLM, `qwen3-coder-30b` on LAN host LeagueOfBeasty at 192.168.1.108:8000, tunneled via bombadil). LLM agents were later expanded to 100 for the 30-day campaign.

The operator confirmed the build order for simulation development steps should be: 2, 1, 3, 4, 5 (scale first, then more rule strategies, then metrics, then LLM agents, then dashboard).

The operator decided that the `adpix` local directory maps to a new project (not `c0inz/surfacemarket`), and that `surfacemarket` is a separate project mapped to `c0inz/surfacemarket`. The `~/projects/adpix` directory was initialized as a fresh git repo, and `c0inz/adpix` was created as a new private GitHub repo.

The operator approved running the `curl | bash` install from `spaiglass.xyz` to connect the VM to the SpAIglass relay.

The operator chose Stellar as the blockchain layer for the GADZ testnet, accepting the assistant's assessment that it is the best fit for payment-grade stablecoin transfers.

The operator accepted the three-instrument capital structure framing: (1) inert consumptive meter token, (2) Reg D / Reg CF fundraising security, (3) eventual Reg A+ public equity. The operator said "sure" to adding the capital roadmap to the testnet playbook.

The operator declared the 30-day learning campaign a major milestone and requested a final milestone report and a launch algorithm report.

The operator requested the SpAIglass connector named `Gadzoom`, agent key named `Gadzoom`, VM connector `Gadzoom`, project name `gadz-sim`, and `Developer` role files for both `gadz-sim` and `gadzoom` projects.

## Open questions

Whether the GADZ protocol should use a real on-chain Stellar asset for the utility token or treat it as a protocol-internal accounting unit only (Option A vs Option B per the session discussion).

Whether the late-campaign profitability plateau (peak day 23, slight sag to day 29) is caused by strategy notes converging on over-aggressive single-surface concentration — not yet investigated.

How allocative efficiency drift (62.6% in the 30-day run vs ~80% in short runs) should be addressed before the testnet.

The plugin-heavy market is the identified soft spot for GADZ owner income — no resolution yet on how to handle this in the live protocol.

Owner-veto dynamics and their potential for griefing were flagged in the testnet playbook but parameters (cap per epoch, refund mechanics) remain open.

Which embedded-wallet SDK to use — Stellar SDK support is narrower than EVM-first options; specific vendor not selected.

Whether to include website ad-space slots in addition to biolink slots for the testnet, and the full agentic-participation scope.

The DV declaration cap question remains open — the session concluded no cap on day one, monitoring `DV ÷ fair_value` in the UI. This is a live monitoring item.

The `TESTNET-PLAYBOOK.md` was committed but the overall testnet build has not started; Phase A.1 decisions (chain, custody model, owner-veto semantics, win condition, entry gate) are still formally open.

## People mentioned

No individuals by name are mentioned in this session beyond the operator (first-person). No third-party people are named.

## Dates and deadlines referenced

Testnet duration: one week (proposed for the community participation test, no calendar date set).

30-day simulation campaign: ran for 6 hours 4 minutes wall time, covering 30 simulated days, completing within the session.

3-day validation run: ran for 34 minutes 47 seconds wall time, completed within the session.

Year 2–3 reference: the operator's rough staging for a Reg A+ public equity raise, mentioned as a multi-year plan.

5-year gameplan: the operator stated a plan to offer public company ownership within a 5-year window; no fixed calendar date.

## Thoughts not yet in the canvas

The session produced a major simulation milestone — the 30-day all-LLM learning campaign — with the following findings not yet reflected in any project canvas: (a) the market self-corrected from 26% to 63% profitable agents over 30 days via nightly LLM reflection; (b) DV over-declaration compressed from 39× to 7× through P&L pressure alone; (c) surface-owner income over 30 days was $88,788 (~$2,960/day), still elevated due to residual over-declaration; (d) allocative efficiency fell to 62.6% due to high churn from P&L-focused learning.

A specific finding about TaxArb strategy: it is identified as genuinely broken in the GADZ market — declaring minimum DV causes force-buy before revenue accumulates, making it universally unprofitable. This is a protocol design insight.

The structural single-controller-per-surface constraint limits total ad value extraction to roughly 60% of a competitive per-demo exchange; the plugin-as-floor model partially offsets this. This finding is documented in `DESIGN.md` and `MILESTONE-REPORT.md` but is a key protocol-level insight for the testnet and mainnet design.

The `spaiglass.xyz/setup` connection process exposed three beta bugs in the SpAIglass product: (1) `/api/projects/register` with `role:"developer"` silently overwrites existing role files; (2) the role template ships with a stale model ID; (3) the `"added": false` response field is ambiguous when a role file is overwritten. These are product feedback items for the SpAIglass codebase.

The consumptive-token design (mint-and-burn, non-holdable, non-transferable) was developed at length. The operator accepted the framing but still wants to explore the initial token as a fundraising vehicle; the resolution was to use Reg D / SAFE + token warrant rather than a public token sale. This is a capital strategy decision that should be recorded in the project's planning documents.

The operator's 5-year equity tokenization plan was flagged as requiring a strict firewall from the meter token narrative to avoid securities integration risk. The playbook now contains this firewall, but the operator's intent to eventually do a tokenized equity offering via Reg A+ is a long-horizon strategic item not yet in any canvas milestone.

## Status changes implied

No formal track/milestone slugs exist in the canvas snapshot (§7 is empty), so no slug-level status changes can be recorded. The following implied status shifts are captured descriptively:

gadz-sim simulation: moved from active development to milestone-complete; the 30-day learning campaign finished and two reports were committed (`MILESTONE-REPORT.md`, `LAUNCH-ALGORITHM-REPORT.md`) to the `feat/demographics-and-revenue` branch, PR #1 at `c0inz/gadz-sim`.

gadz-sim testnet playbook: moved from draft to committed and updated with capital roadmap, Stellar chain decision, non-custodial wallet architecture, and no-escrow settlement design.

SpAIglass connector for the `readystack` VM: moved from not configured to active — connector `Gadzoom` is online, both `gadz-sim` and `gadzoom` projects registered, Developer role files in place, `spaiglass.service` active.

GitHub credentials on the `readystack` VM: a GitHub classic PAT (value redacted) was stored to `~/credentials/github.json` as `pat_classic_write`, replacing dead credential entries. The spaiglass credential helper remains broken and requires separate remediation.

