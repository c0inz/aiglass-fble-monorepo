---
schema_version: 1
audit_id: 2026-06-10T12-01-01-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/workflows/wf_d588297d-c49/agent-a439b0137f793073d.jsonl
  session_started: '2026-06-10T20:57:28.334Z'
  session_ended: '2026-06-10T21:00:05.657Z'
  session_size_bytes: 118303
  truncated: false
---

## Decisions made

The operator (John) submitted two competing Alt1 designs ("FULL ORNAMENT" and "WAINSCOT") for evaluation against the tabled Plan v1 (Fact Harness). The direction to use real Postgres columns for tenant fields — not EAV rows, not a projection table — was previously decided by John and is treated as settled. The session asked an AI agent to score both designs 1–10 through the lens of migration realism and release risk on live production tenant BRI0, pick a winner, and identify grafts from the losing design. The agent tool calls in the session did not produce a visible scoring verdict before the session ended; no winner was formally declared in the visible transcript.

The designs share the following structural decisions already treated as locked: a per-tenant 1:1 ornament table (`ai_venture_ext`) separate from `ai_venture`; `am_field_spec` survives as the column registry; DDL is routed exclusively through `lib/ornament.js`; venture-anchored EAV property rows are deleted at Phase 3 (irreversible, gated on `pg_dump` and explicit owner approval); form/WO capture rows stay as EAV permanently (different grain); a reject-trigger on `ai_field_entry` enforces loud failure for column-backed specs post-cutover; nightly reconcile with an in-product heartbeat addresses the documented 43-day cron death pattern.

The two designs differ primarily in how promoted workflow facts are governed: Design 1 uses a `origin='project_fact'` flag on `am_field_spec` and a GUC-based write guard enforced by a trigger; Design 2 uses a separate `am_promoted_fact` table with declared by-ID bindings and a strict no-manual-writes rule enforced in `lib/facts.js`.

## Open questions

No winner between Design 1 ("FULL ORNAMENT") and Design 2 ("WAINSCOT") was declared in the visible session transcript — the agent began tool calls but the session ended before a scored verdict was output.

It is unresolved which design's promoted-fact governance approach (GUC trigger on `am_field_spec` vs separate `am_promoted_fact` table) John prefers.

The companion `internal_name`-on-blueprints work (mig-164 pattern for stage and WO blueprints) is acknowledged as necessary by both designs but is not explicitly scheduled beyond "alongside Phase 4."

Whether Phase 6 (retiring task_virtual mirrors, 3,235 specs, 35,838 rows) will be pursued is explicitly deferred and optional in both designs.

## People mentioned

John (owner; previously rejected Plan v1 and set the columns direction).

## Dates and deadlines referenced

No specific calendar dates or deadlines were referenced in this session. All phasing references are relative (Phase 0 through Phase 5 or 6) with no calendar dates attached.

## Thoughts not yet in the canvas

Both designs explicitly note that 83 of 695 BRI0 property specs already have `internal_name` populated; the remaining 612 require a one-shot minting script in Phase 0. This minting scope is a concrete pre-work item not reflected in any existing milestone.

Both designs enumerate a CI grep-gate that would fail the build on new occurrences of `label ILIKE`, `def_group_label IS DISTINCT FROM`, or `title ILIKE` in routes/lib files, treating label-matching as lint-enforced impossible to reintroduce. This is a new CI enforcement mechanism not captured elsewhere.

Design 1 proposes a new `ai_ext_audit` append-only log table (migration 163). Design 2 proposes the same table as optional-but-recommended. Neither design maps this to an existing milestone.

Both designs reference a `DATA_DICTIONARY` that would regenerate from the registry so agents always see live column-to-label-to-kind maps. This is a documentation/tooling output not in the current canvas.

The operator's DB connection details were present in the session blob (host, port, dbname, user, and a reference to a `.env` file containing a password). The password value itself was not displayed in the transcript and is not reproduced here. The connection string pattern is redacted per §3.1.

An agent tool call attempted to read a database password from the `.env` file via a shell command. The password value was not echoed in the visible output. Value redacted.

## Status changes implied

No explicit status changes can be inferred. Both designs are in PROPOSED state. No milestone was moved from planned to active in this session.

