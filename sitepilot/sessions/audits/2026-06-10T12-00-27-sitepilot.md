---
schema_version: 1
audit_id: 2026-06-10T12-00-27-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/workflows/wf_2c50bcf2-56e/agent-acdfd96c824723174.jsonl
  session_started: '2026-06-10T06:55:19.359Z'
  session_ended: '2026-06-10T07:03:06.147Z'
  session_size_bytes: 221258
  truncated: false
---

## Decisions made

No final product or engineering decisions were made during this session. The session was an adversarial review of a proposed plan (the Fact Harness) submitted for critique, not approval. The review produced a verdict of "holds-with-fixes" — the derived-projection architecture was found to survive adversarial scrutiny, but the operating model as written was judged to fail the assigned attack vector (operating cost and human factors).

## Open questions

- Whether the operating model can be fixed to address silent coverage decay with only pull-based detection (flagged as fatal-class).
- Whether an unmonitored load-bearing cron is acceptable given the org's documented 43-day dead-cron history and deferred production monitoring (flagged as fatal-class).
- Whether the provenance gaps in `ai_fact_point_log` (no actor/cause column) and `ai_fact_pin` (`pinned_by` colliding worker-id and api-key-id spaces) will be resolved before Phase 2 ships.
- Whether all five unwired script-tier `ai_field_entry` writers — including `scripts/sync/sync-form-blueprint.js:217` — will be enumerated and wired before the Phase 2 acceptance gate.
- Whether `routes/addons.js` (the actual location of route-tier `ai_site_visit` writes) will be identified as a chokepoint in the plan rather than the incorrectly named schedule routes.
- How the declaration UI burden on EPC tenant admins will be managed to prevent registry rot over time.
- Whether per-tenant blueprint divergence will create binding maintenance overhead at scale.

## People mentioned

No individuals by name are mentioned in this session. The session references "the owner" and "senior dev" as roles, plus SUPERADMIN and BRI0 as system/tenant identifiers.

## Dates and deadlines referenced

- 2026-06-10: Date the plan's repo facts were stated to have been re-verified by the plan author.
- 2026-06-18: Example value for `installation_start_date` used in the plan's worked example (illustrative, not a deadline).
- 2026-06-01: Lower bound in an example API filter query (illustrative, not a deadline).
- No explicit project deadlines or release dates were stated.

## Thoughts not yet in the canvas

- The adversarial review identified 16 actual `ai_field_entry` writers in the repo versus the plan's claimed 11; five unwired scripts were enumerated, including `scripts/sync/sync-form-blueprint.js:217` which mutates spec identity and `touched_at`. This factual discrepancy in the plan's "re-verified" claims is not reflected in any existing canvas milestone.
- Route-tier `ai_site_visit` writes live in `routes/addons.js`, not the schedule routes as stated in the plan. This is a concrete factual error in the plan that needs correction before Phase 2 chokepoint wiring.
- The org has a documented 43-day dead-cron incident history. The plan's nightly rebuild+reconcile cron is load-bearing from Phase 1, but the plan includes no monitoring or alerting for cron health. This operational gap is not captured in the canvas.
- The `am_api_idempotency` machinery (mig 159) is described as "dead" in the plan but is relied upon by the PATCH pin endpoint. Its actual state in the repo was checked by the reviewer but no canvas item tracks its activation.
- The plan lacks an actor/cause column in `ai_fact_point_log`, and `pinned_by` in `ai_fact_pin` conflates worker-id and api-key-id namespaces — both provenance gaps flagged as majors with no corresponding canvas items.
- The suggestion wizard (label-inference at declaration time, human-confirmed) has no canvas milestone tracking its build or acceptance criteria.
- The "Apply historically" button per binding (admin-facing face of `rebuild.js`) has no standalone canvas milestone.
- The conflict queue UI ("prefer this source," pin-vs-capture disagreement list) has no standalone canvas milestone.
- The integrator docs page described as "the first user-facing API doc" (Phase 4, GAP 8 for facts) has no canvas entry.
- The standing code-review rule ("new label-reading code fails review") has no enforcement mechanism described in the canvas.

## Status changes implied

No status changes to existing canvas milestones are implied. The session was a design review of a proposed plan; no work was confirmed as started, completed, or blocked on a tracked milestone.

