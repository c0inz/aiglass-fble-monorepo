---
schema_version: 1
audit_id: 2026-06-10T12-00-31-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/workflows/wf_2c50bcf2-56e/agent-a735c4603842474bd.jsonl
  session_started: '2026-06-10T06:44:35.639Z'
  session_ended: '2026-06-10T06:59:19.399Z'
  session_size_bytes: 477357
  truncated: false
---

## Decisions made

The Fact Harness plan (Design 2 skeleton with grafts from Designs 1 and 3) was submitted for adversarial review. The reviewer's verdict was "holds-with-fixes" — the core registry/bindings/projection/pin architecture survives the attack, but specific claims were refuted and the headline assertion that label inference dies everywhere except the wizard was not upheld.

The plan's data model decisions that were not overturned: `am_fact_def` (fact registry), `am_fact_binding` (source declarations), `ai_fact_point` (strictly derived projection, single writer), `ai_fact_pin` (capture-tier overrides, separate from projection), `ai_fact_point_log` (append-only history). Immutable `fact_key`, deactivate-never-delete policy, frozen source-kind set, and pin-table separation from the projection were all accepted as structurally sound.

No decision was made to proceed to implementation; this session was review only.

## Open questions

Label inference survival in the automation tier is unresolved. `lib/automation/engine.js` resolves `property:` conditions via `fs.label ILIKE` with `LIMIT 1`, stage conditions compare label strings stored in rule JSONB, and `checklist_item_contains` keyword-matches labels. These cannot be eliminated by the plan as written.

`routes/agents.js` exposes a generative LLM rule-builder that mints new `property:label_name` references as tenant data, which CI grep and code review cannot intercept. No fix was proposed.

Schedule categorize, dashboard kanban grouping, and digest section 2 (service tickets with `venture_ref=NULL`) are task-grain or per-event and cannot be expressed in a project-grain single-row registry. Whether these will remain as inference or be addressed by a separate mechanism is unresolved.

Stage-timing facts are unrepresentable: `ai_stage_instance` has no `completed_at` column and there is no `stage_scalar` source kind in the frozen set. Whether to add `completed_at` or unfreeze the kind set is unresolved.

`TIMERANGEDAYS` and `is_multiple` field types, multi-instance blueprints, and ACCOUNT-grain customer facts cannot be declared in the registry. No resolution was discussed.

The `ai_field_entry` schema lacks a functioning `touched_at` update path. `lib/task-virtual-entry.js` and the property-save route do not bump `touched_at` on UPDATE (the route comment wrongly claims the columns do not exist). This means "priority, then recency" survivorship arbitrates the 444 duplicate-spec twins on dead timestamps, risking the stale twin being promoted with `state='OK'`. No fix was decided.

Pins have no DELETE endpoint or UI. Whether this is intentional or a gap is unresolved.

Fresh-tenant pre-binds (provisioner seeding shipped template blueprints) depend on stable blueprint identity. The reviewer noted this is circular without pulling mig-164 `internal_name` onto the critical path; this dependency was not resolved.

Whether to add a `stage_scalar` source kind and `completed_at` on `ai_stage_instance` to support stage-timing facts was not decided.

## People mentioned

No individuals were named by first name or role in this session. The session is a developer/operator interaction with an AI adversarial reviewer; no named persons appear in the inputs.

## Dates and deadlines referenced

2026-06-10: The plan states repo facts were re-verified on this date (migrations top at 162, 11 files write `ai_field_entry`, `server.js` has no transaction wrapper, digest ILIKEs at specified lines). The reviewer refuted three of these four claims.

Phase timeline referenced in the plan (not externally committed dates): P0 2 days, P1 2–3 days, P2 5–6 days, P3 4–5 days, P4 3 days, total approximately 4 weeks to Phase 4.

2026-06-18 and 2026-06-20: example `installation_start_date` values used in the worked example and API sample response; not real project dates.

2026-06-01: example filter value in the Agent API list-filtering endpoint sample; not a real deadline.

## Thoughts not yet in the canvas

The adversarial reviewer identified that the writer count for `ai_field_entry` is 14, not 11. The three missing writers are `sync-form-blueprint.js`, `backfill-definitions.js`, and `merge-dup-property-specs.js`. P2 of the plan does not wire these files.

The reviewer confirmed `ai_site_visit` is written by `routes/addons.js`, not "schedule routes" as stated in the plan. The plan's P2 chokepoint list names the wrong file.

The "no transactions today" premise in the plan is false. `middleware/tenant.js` provides a per-request client, and `q('BEGIN')` is already used in `routes/forms.js` and `routes/projects.js`. The plan's consistency framing (write-through as "best-effort" because no transactions exist) is based on an incorrect premise.

The suggestion that BEGIN/COMMIT wrappers are "optional later hardening, not load-bearing" may need revisiting given that transactions are already in use in the codebase.

The plan's "23-site audit" (23 places of label inference) was referenced but not verified in this session against an actual audit document.

## Status changes implied

No milestone slugs from the canvas were referenced. No status changes are implied by this session; the session was adversarial design review only, with no implementation work started or completed.

