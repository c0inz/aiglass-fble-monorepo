---
schema_version: 1
audit_id: 2026-06-10T12-01-16-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/agent-abf3a2bca2d40995b.jsonl
  session_started: '2026-06-04T20:36:18.424Z'
  session_ended: '2026-06-04T20:38:34.773Z'
  session_size_bytes: 373576
  truncated: false
---

## Decisions made

No explicit decisions were made in this session. The operator submitted a research and investigation request, not a decision-making conversation. The assistant produced a structured technical audit of the codebase; no decisions about implementation approach, timeline, or scope were confirmed by the operator.

## Open questions

The operator is writing a feature spec for a form and work-order versioning/editing system. The following design questions remain open based on the investigation findings:

- Whether versioning will be implemented via a separate `am_questionnaire_version` table or by adding version columns directly to `am_questionnaire_blueprint`.
- How field reordering will be surfaced in the UI (drag-and-drop via jQuery UI Sortable or native HTML5 Drag API).
- Whether "disable" (soft-delete) will replace hard-delete routes or coexist alongside them.
- Which version is considered "active" for new project instantiation when multiple versions exist (e.g., highest locked version, a separate `is_active` flag, or most recently locked).
- Whether work orders (`am_assignment_blueprint`) will get the same versioning treatment as forms (`am_questionnaire_blueprint`) in the same spec or a follow-on spec.

## People mentioned

No people were named or referenced in this session.

## Dates and deadlines referenced

No dates or deadlines were referenced in this session.

## Thoughts not yet in the canvas

The operator is preparing a feature spec covering four capabilities not currently implemented in sitepilot:

1. **Field reordering**: No `position` column exists on `am_field_spec` today; fields render in `id` order. Building reorder requires a migration adding a `position` column, updates to the instantiation library (`lib/eav-instantiate.js`), updates to the PUT field route (`routes/settings.js:3681`), and a sortable UI in the form editor.

2. **Disable/hide fields and groups (soft-delete)**: No `is_disabled` or `is_archived` flag exists on `am_field_spec` or `am_field_arrangement` today. Hard-delete routes exist. Building soft-delete requires a migration, changes to DELETE routes to flip flags instead of removing rows, changes to render queries to filter disabled rows, and new UI states.

3. **Edit field prompt/help text**: The `am_field_spec.notes` column exists, the `def_notes` frozen column was added in migration 158, the form editor has a "Help Text" input, and the PUT route already accepts `notes`. This capability is already fully wired and requires no new build work.

4. **Form versioning (draft → lock lifecycle)**: No versioning concept exists anywhere in the codebase. No `version`, `is_draft`, `is_locked`, `published_at`, or `parent_ref` columns exist on any form or work-order table. Migration 124 explicitly stripped lifecycle columns from blueprints. Implementing versioning is a greenfield effort requiring: a new version entity (table or columns on blueprint), immutability enforcement on locked versions, a copy-on-edit flow, UI state for Draft vs Locked, a "Lock Version" action, and a backfill migration for existing templates.

The assistant confirmed that the EAV instantiation design (`lib/eav-instantiate.js` + migration 158) already enforces template/instance isolation: existing `ai_field_entry` rows with `instantiated_at IS NOT NULL` are never affected by blueprint edits. This is relevant context for the versioning spec.

Work orders (`am_assignment_blueprint` + `ai_task_instance`) use the same field/arrangement model as forms and share the same editor; any versioning spec for forms will need to address whether work orders are in scope.

## Status changes implied

No status changes to existing canvas milestones are implied by this session. The session was a codebase investigation to inform a forthcoming feature spec; no implementation work was started or completed.

