---
schema_version: 1
audit_id: 2026-06-10T12-00-12-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/agent-af9ac4f017407d9ab.jsonl
  session_started: '2026-06-02T03:31:21.378Z'
  session_ended: '2026-06-02T03:33:49.349Z'
  session_size_bytes: 350092
  truncated: false
---

## Decisions made

The operator decided to conduct a comprehensive ground-truth inventory of the SitePilot server-side API surface as a prerequisite step before writing a V1 REST API spec. No architectural or feature decisions were finalized in this session; the inventory is explicitly scoped as research only ("Don't write the spec — just give me the comprehensive ground-truth inventory").

## Open questions

1. Which of the customer's 15 feature request areas require greenfield data modeling vs. a JSON layer over existing routes? The inventory identifies four fully missing areas (Proposals/Contracts, Permitting/Interconnection, Inventory/Equipment, Reporting/Analytics) but does not resolve prioritization.
2. What authentication mechanism will be added for programmatic API access? No Bearer token, API-key, or OAuth2 flow exists today; session-cookie-only auth blocks external REST consumers.
3. How will CSRF exemption or explicit CSRF-token header checking be handled for `/api/` paths once non-browser clients are admitted?
4. Will outbound webhook delivery be added, and if so, will it attach to the existing `am_notification_receiver` table or require a new webhook-delivery table?
5. Error response format is not yet standardized across routes; the spec work will need to decide on a canonical shape.
6. No request-ID or distributed tracing middleware exists; whether this is in scope for V1 is unresolved.

## People mentioned

No individual people were named or referenced in this session.

## Dates and deadlines referenced

2026-06-02: The inventory is noted as "ground truth as of 2026-06-02" (the date of the session). No deadlines or milestones with dates were referenced.

## Thoughts not yet in the canvas

- The codebase has a `GET /settings/keys` route and a full API key CRUD surface (`/settings/api/keys/groups/*`, `/settings/api/keys/*`) already present in code but described as "reserved for future; no UI reads these today." This is a significant latent capability not reflected in any current canvas milestone.
- The export routes (`/api/exports/*`) already return canonical, fully-formed JSON object graphs for projects, accounts, sites, workflows, forms, work orders, roles, teams, and workers. These may be immediately reusable as read endpoints for an API V1 without a separate JSON layer.
- The `routes/agent-chat.js` surface (LLM copilot chat + history) is noted in the route inventory but not detailed; this may represent a separate product capability worth tracking.
- The `ai_inbox` legacy table is confirmed deprecated and no longer read as of PR #28; the replacement is `ai_inbox_event` + `ai_inbox_recipient` (PR #16 rebuild). This migration state may be relevant to any documentation or integration work.
- Notification channels for push and SMS exist in the JSONB channel schema but are reserved for future use and not exposed in the UI.
- The `ai_crew` (teams) export includes only the crew row itself; member junction table data is absent from the export, which may be a gap for API consumers.
- The `am_notification_rule` + `am_notification_receiver` tables represent an admin-configurable notification policy layer that is editable via JSON API at `/settings/api/notifications/rules/:id`.

## Status changes implied

No status changes to existing canvas milestones are implied by this session. The session is exploratory/research in nature.

