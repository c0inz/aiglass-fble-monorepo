---
schema_version: 1
audit_id: 2026-05-29T18-00-14-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-05-29T18:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6.jsonl
  session_started: '2026-05-06T17:15:54.507Z'
  session_ended: '2026-05-30T02:24:33.737Z'
  session_size_bytes: 281423123
  truncated: true
---

## Decisions made

The operator decided to wipe BRI0 and re-pull all three Brighthouse accounts (615611, 693903, 694215) fresh from Coperniq using the canonical extractor, rather than patching the partially-loaded state.

The operator decided the prod master suite should run against a fresh test tenant (AIEPC_MAS1), not against BRI0, and confirmed this is how the suite already works.

The operator decided to let the prod master suite finish rather than abort it when the purpose was questioned mid-session.

The operator decided to fix the C1 critical security finding (port 8090 world-open file mirror) immediately.

The operator decided to implement the easy security fixes (H2, M1, M2, M4) in one batch after the prod suite finished, and to document all findings in a security audit document.

The operator decided to defer H1 (unauthenticated /files/* route) pending a decision on whether notification emails embed image URLs.

Migration 123 was applied: all 70 ON DELETE CASCADE foreign keys in BRI0 and MAS0 were converted to NO ACTION.

The phase-by-phase UI sweep strategy was adopted: Phase 1 covered four primary browse-path route files, Phase 2 covered lib engines and remaining route files, with a single deploy per phase.

The column mapping from old schema to new schema was confirmed: ai_step_instance.lifecycle maps to ai_task_instance.status, sealed_at maps to completed_at, assignment_ref maps to template_assignment_ref, questionnaire_ref maps to template_questionnaire_ref. ai_work_ticket_old maps to ai_task_instance WHERE type='WORKORDER' AND location_type='SITE_SERVICE'.

The Coperniq batch export driver was configured to run batches 1–23 at 8-minute intervals, Step 1 only (Coperniq to staging, not BRI0), using scripts/parity-test/run-all-batches.sh.

The C1 fix was verified live: external access to the file mirror at 64.23.183.9:8090 blocked (000/timeout), app proxy still serving files via the retained app-droplet rule.

All four security fixes (H2, M1, M2, M4) were verified live on prod after deploy.

The prod master suite completed fully green (all 12 specs, 0 failures) validating the V1 promote.

## Open questions

Whether notification emails embed image or file URLs — answer determines H1 fix approach (signed URLs vs. simple auth gate on /files/*).

Whether DO Account-0 token ("Personal — 4 droplets") is intentionally revoked or represents 4 unaudited droplets (M7).

When maintenance windows for kernel reboots on all three droplets can be scheduled (M3).

Whether to proceed with Cloudflare proxy cutover for app.sitepilot.work (M5).

Whether to prune the expired dotty SSH key on the app host and the duplicate SSH key entries on app and asset hosts (M6 — operator approval required before removals).

Whether CSRF tokens on state-changing forms should be implemented now (M2 partial — SameSite done).

## People mentioned

Isaac Rosa (Coperniq account owner field, account #544 Ismael Bejarano).
Keaton Garner (Coperniq account owner field, account #306 Michael Smith).
Tom Davenport (Coperniq account owner field, account #243 david howe).

## Dates and deadlines referenced

2026-05-04: Date the Coperniq token in .secrets/coperniq-token.json was last refreshed.
2026-05-18 23:49 UTC: Coperniq token expiry (confirmed 12 days of headroom at time of check, well past by session date).
2026-05-06: Checkpoint commit date for in-flight state before UI sweep.
2026-06-06: Gate date for migration 103 (final _old table drop) — must not run before this date.
2026-03-17: Expiry date embedded in the dotty SSH key comment on the app host (expired ~10 weeks before the audit).

## Thoughts not yet in the canvas

The blast radius of the schema refactor was larger than the workplan described: 19 files referenced ai_step_instance and 14 referenced ai_work_ticket_old, not just 3 files. The workplan's estimate of 2–3 hours was described as underselling the work by the assistant.

The headless crawler (scripts/crawl-bri0.js) and scripts/peek-project.js, scripts/peek-step.js, and scripts/dump-step.js were created as new development tooling for automated UI verification.

A prod master suite run can be blocked by fail2ban if audit-style path probes are run against prod from the same VM during the suite. The nginx-noscript jail bans the VM IP and causes ERR_CONNECTION_REFUSED mid-suite. The mitigation is to run security probes and prod suite runs from separate VMs or in separate time windows. The unban command sequence was documented in SECURITY.md and in cross-session memory.

The local .env points at 127.0.0.1:15432/ecpwork (dev), not the prod DB. The prod DB is on DO private network 10.124.0.2 (internal hostname db.agentepc.internal:5432), tunneled via the app host at 143.110.232.40.

scripts/wipe-acct.js had a bug with field_spec/field_arrangement deletion after the CASCADE removal (mig 123): it attempted to delete specs shared across accounts, which failed without CASCADE. The fix was to leave specs/arrangements alone and let the loader UPSERT them.

The loader (scripts/load-one-account.js) was missing hydration for process_ref, active_stage_ref, serial_number, owner, manager, and address fields on projects. These were patched and accounts reloaded.

routes/forms.js had a post-Phase-1 bug: two queries against ai_event_stream referenced es.assignment_ref, which does not exist on that table (it has step_instance_ref). This caused 500 errors on all 179 step detail pages. Fixed and redeployed.

The form renderer was also broken because am_field_arrangement lost arrangement_kind/caption/attribute_ref columns in the refactor; those moved to am_field_spec. The forms.js route was patched to query from am_field_spec instead.

The project list showed only 2 of 4 projects under the default filter, and project titles appeared as "untitled." Both were route-level filter and heading-fallback issues fixed in routes/projects.js.

The spec 91 failure during the local suite (priority field PUT returning non-ok) was diagnosed as a transient flake in the rapid-fire save sequence, not a server defect. The endpoint was confirmed correct by direct reproduction. The fix was to add a hydration wait and a single retry to the setControlAndWaitForSave helper in spec 91.

The promote sequence for the V1 audit-resolution branch involved: push to main, deploy.sh on app host (143.110.232.40), apply migrations 156/157 to all 21 prod tenant schemas on the DB host (64.23.183.9), reseed notification rules across all 21 schemas (23 rules + 33 receivers each, including new TICKET rules).

docs/SECURITY.md was created as a living security audit document covering: a verified-good baseline, a findings ledger, audit history table, and a reusable audit checklist. It is committed to main and deployed to prod.

The security audit identified that the app server, DB server, and asset server all need reboots to activate staged kernel patches. No pending package updates otherwise.

The npm audit fix resolved the two high-severity advisories: js-cookie (prototype hijack) and path-to-regexp (ReDoS). Post-fix: 0 vulnerabilities. Route matching was verified intact after the path-to-regexp version bump.

DO Account-0 with note "Personal — 4 droplets" returned a 401 on all API calls, making those droplets unauditable in this session.

The app at app.sitepilot.work is not behind the Cloudflare proxy (grey-cloud/DNS-only), exposing the origin IP 143.110.232.40 directly.

App-internal services are bound to 0.0.0.0 (ports 3010, 4201, 5000, 40081 on app host; 8080, 8100 on DB host) — the firewall is the real gate, but private-IP binding was noted as better defense-in-depth.

## Status changes implied

No canvas slugs are defined in the playbook (§7 is empty). The following changes are implied but cannot be mapped to existing slugs:

The V1 audit-resolution promote is complete: branch merged to main at commit 1e31c28, deployed to prod, migrations 156/157 applied to all 21 tenant schemas, notification rules reseeded, prod master suite green (all 12 specs).

The Coperniq batch export pipeline is actively running: batches 1–23 at 8-minute intervals, targeting AIEPC_PARITY_TEST staging (not BRI0), with batch 7 of 23 confirmed running at session mid-point.

Security finding C1 (file mirror port 8090 world-open) status changed from OPEN to FIXED within the same session.

Security findings H2, M1, M2, M4 status changed from OPEN to FIXED within the same session, with verification on prod.

Security finding C1 detailed: the 0.0.0.0/0 inbound rule for TCP 8090 on db-server-firewall (firewall ID redacted) was removed via the DigitalOcean API. The app-droplet-restricted rule was retained.

