---
schema_version: 1
audit_id: 2026-06-10T12-01-18-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/agent-a7752f7678f1497dc.jsonl
  session_started: '2026-06-08T21:14:42.202Z'
  session_ended: '2026-06-08T21:16:21.344Z'
  session_size_bytes: 351669
  truncated: false
---

## Decisions made

No explicit decisions were made in this session. The session was a read-only investigation with no commits, no configuration changes, and no directives to implement anything.

## Open questions

1. Whether to enable the existing "Financials" tab (currently disabled with `opacity:0.55` and `title="Coming soon"` in `views/projects/detail.ejs:288-290`) — the stub exists but activation has not been decided.
2. Whether financial properties should surface under the Properties tab (automatic once a "Financial Properties" group is created) or under a dedicated Financials tab requiring additional view work.
3. There is a legacy/draft financials panel in `detail.ejs` (lines 833-916) wrapped in `<% } else if (false && tab === 'financials_legacy') { %>` — whether to discard or revive it is unresolved.

## People mentioned

No people were mentioned by name in this session.

## Dates and deadlines referenced

No dates or deadlines were referenced.

## Thoughts not yet in the canvas

The session produced a detailed map of how to add a "Financial Properties" group and potentially activate the Financial tab. Key findings not tied to any existing canvas slug:

- The project detail page already has 6 tabs hardcoded in `views/projects/detail.ejs:274-297`: Work, Properties, Docs, Financials (disabled), Activity, Reminders.
- Tabs are not driven by config — they are static HTML in the EJS view. Enabling the Financials tab requires removing the stub styling and adding a content block.
- Project properties use a 3-table hierarchy: `am_field_spec` (property definitions), `am_property_group` (group/category), `am_field_spec_group` (many-to-many membership). All schema is in place via migration 149.
- Property values are stored as EAV rows in `ai_field_entry` with `venture_ref` set to the project id and `step_instance_ref`/`stage_instance_ref`/`task_instance_ref` all NULL for project-level properties.
- The project properties admin UI lives at `GET /settings/properties/project` (routes/settings.js:707-755) with CRUD at `POST /settings/api/properties/project`, `PUT /settings/api/properties/:id`, and `DELETE /settings/api/properties/:id`.
- To add a "Financial Properties" category: insert a row into `am_property_group (scope='PROJECT', name='Financial Properties')`, create specs via the admin UI, and link them via `am_field_spec_group`. No code changes are required for the Properties tab to render the new group automatically.
- The route handler fetches `ai_field_entry` values grouped by `def_group_label` (routes/projects.js:910); the view renders all groups in order (detail.ejs:649-697).

## Status changes implied

No status changes are implied. The session was investigative only; no milestones were started, completed, or moved.

