---
schema_version: 1
audit_id: 2026-06-10T12-00-52-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/workflows/wf_d588297d-c49/agent-a5dcd9b4e92959add.jsonl
  session_started: '2026-06-10T20:42:06.444Z'
  session_ended: '2026-06-10T20:51:25.977Z'
  session_size_bytes: 313717
  truncated: false
---

## Decisions made

John's previously stated direction for Alt1 was reaffirmed as the constraint for this session: real Postgres columns for tenant-specific fields, added as needed, rather than rows or a projection table. Plan v1 (the Fact Harness with registry, bindings, and derived projection table `ai_fact_point`) remains tabled and was not revisited.

The agent was tasked as a feasibility auditor for the custom-columns ornament approach, not to propose a new design.

## Open questions

No explicit open questions were stated by the operator. The session was scoped as a feasibility audit with findings to be compiled and returned, not a decision-making session.

It is not resolved whether properties will actually move to columns — the session was an audit of what that would require, not a commitment to do it.

## People mentioned

John (owner/operator — previously rejected Plan v1 and set the Alt1 direction).

## Dates and deadlines referenced

No dates or deadlines were referenced in this session.

## Thoughts not yet in the canvas

The agent conducted live database measurements against the prod tenant `AIEPC_BRI0` schema to quantify: how many `(venture, spec)` pairs have multiple `ai_field_entry` rows; how many distinct `project_property` specs have any value; and max per-tenant property count. These measured numbers were compiled into the audit output but the specific values are not captured in this session blob (the tool output content was not included in the blob).

The agent also audited which code paths read and write `ai_field_entry` for `project_property` origin entries, identifying relevant files in `routes/projects.js`, `routes/forms.js`, `routes/agent-api/`, `scripts/sync-coperniq.js`, and `lib/visibility-engine.js`. The question of which of these survive untouched if only properties move to columns was part of the audit scope.

The agent noted that `FILE`-kind property values live in `ai_attachment` rather than scalar columns in `ai_field_entry`, a distinction relevant to the column-migration scope.

The agent empirically timed `ALTER TABLE ADD COLUMN` mechanics on the prod connection using a session-local temp table to measure lock level and rewrite cost.

The prior-round artifacts referenced — `design-3.md`, `judges.json`, `attacks.json`, and `FACT_HARNESS_EVIDENCE.md` — are stored in `/tmp/alt1-inputs/` and remain active inputs. The 23 label-inference sites catalogued in `FACT_HARNESS_EVIDENCE.md` are treated as a standing requirement: any design must eliminate label-matching as the consumer discovery path.

The recorded objections against `design-3.md` (canonical-store design) — non-recomputable and missed-hook = silent corruption — were stated as constraints the Alt1 ornament design must confront.

DB host detail: the prod Postgres DB is on a separate host (`db.agentepc.internal`), not on the app server (`143.110.232.40`). An SSH tunnel through the app box to the DB host was required for the agent to run queries.

## Status changes implied

No milestone status changes are implied by this session. The session was a feasibility audit with no canvas milestones referenced by slug.

