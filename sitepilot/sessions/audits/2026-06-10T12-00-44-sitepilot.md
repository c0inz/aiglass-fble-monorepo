---
schema_version: 1
audit_id: 2026-06-10T12-00-44-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/workflows/wf_c37bc2ed-2e8/agent-ad303c3b5fd45ffc8.jsonl
  session_started: '2026-06-05T00:10:27.766Z'
  session_ended: '2026-06-05T00:30:46.530Z'
  session_size_bytes: 1162055
  truncated: false
---

## Decisions made

The prior-pass claim that step dependencies are frozen and do not propagate to existing projects after blueprint edits was formally refuted. The definitive answer established by the trace: a project created before a blueprint dependency edit WILL have its blocking behavior updated when an admin edits step dependencies through the SitePilot settings UI, because `routes/settings.js` explicitly propagates both adds (lines 4473-4483) and deletes (lines 4507-4513) to all existing ventures' `ai_step_dependency` rows.

The legacy `lib/dependency-engine.js` and `lib/phase-engine.js` (which operate on the `element_*` schema) were confirmed to be dead code, intentionally disabled as of 2026-05-13 per the comment at `routes/projects.js:24-27`.

## Open questions

Propagation only reaches ventures where both blueprint steps were instantiated via the `step_blueprint_ref` join. Whether there are ventures where steps were not instantiated — and therefore would not receive blueprint dependency edits — was noted as a caveat but not fully enumerated.

Direct SQL edits to `am_step_prerequisite` that bypass `routes/settings.js` would not propagate to existing projects. Whether this bypass path is used in practice (e.g., by migrations) was not resolved.

## People mentioned

No named individuals mentioned in the session blob.

## Dates and deadlines referenced

2026-05-13: Date the legacy prerequisite/phase engine was intentionally disabled, as recorded in the comment at `routes/projects.js:24-27`.

## Thoughts not yet in the canvas

The session revealed that two separate dependency systems exist in the codebase: the current live system (`ai_step_dependency` / `am_step_prerequisite`) and a legacy system (`element_dependencies` / `element_instances` / `element_templates`) that is no longer wired into the live request path. This legacy code presence in the repo was flagged during the trace. No canvas milestone exists for removing or formally deprecating the legacy dependency engine.

The StructuredOutput tool was called many times during the session without parameters being transmitted; the tool repeatedly returned empty-body validation errors. This was logged by the stop hook and eventually resolved. The reliability of the StructuredOutput mechanism in this agent session context may warrant investigation.

## Status changes implied

No canvas milestone slugs were referenced in the session. No status changes can be mapped to tracked milestones.

