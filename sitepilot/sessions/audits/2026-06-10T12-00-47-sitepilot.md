---
schema_version: 1
audit_id: 2026-06-10T12-00-47-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/workflows/wf_c37bc2ed-2e8/agent-a754f02d997b0eda6.jsonl
  session_started: '2026-06-05T00:05:01.996Z'
  session_ended: '2026-06-05T00:08:23.439Z'
  session_size_bytes: 296380
  truncated: false
---

## Decisions made

No explicit decisions were made during this session. The session was a read-only investigation of existing schema and code.

## Open questions

1. Whether the vestigial `is_archetype` and `archetype_ref` columns on `am_questionnaire_blueprint` should be removed from the schema or retained for historical reasons.
2. Whether `form_instance_ref` (noted as absent in migrations) needs to be added for a future versioning mechanism.
3. What is missing for a full field-versioning implementation — the session identified that the freeze columns exist but a complete versioning story was flagged as a gap.

## People mentioned

No people were mentioned by name in this session.

## Dates and deadlines referenced

No dates or deadlines were referenced.

## Thoughts not yet in the canvas

- Migration 158 introduced 13 denormalized `def_*` columns on `ai_field_entry` to snapshot field definitions at instantiation time (def_label, def_internal_name, def_data_kind, def_is_mandatory, def_is_multiple, def_is_locked, def_options, def_position, def_group_label, def_group_position, def_settings, def_internal_visibility, def_notes). These are the frozen snapshot columns and are intended to be read-only after instantiation.
- The anchor columns on `ai_field_entry` are `venture_ref` (project), `step_instance_ref` (workflow step), and `task_instance_ref` (task/form), added in migration 144.
- The `field_spec_ref` column on `ai_field_entry` serves as the lineage FK pointing back to the live `am_field_spec` row.
- Blueprint tables confirmed: `am_questionnaire_blueprint`, `am_field_spec`, `am_field_arrangement`, `am_assignment_blueprint`, `am_property_group`, `am_field_spec_group`. Field groups added in migration 149, which also added `internal_name` to `am_field_spec`.
- `is_archetype` and `archetype_ref` on `am_questionnaire_blueprint` (added in migration 002) are vestigial — no active code path reads them. They are legacy from the original Coperniq system.
- `ancestor_assignment_ref` on `am_questionnaire_blueprint` is the active inheritance mechanism — it points to parent work-order/task blueprints and is used in routes/projects.js around line 2425 to auto-provision child forms when a work order is created.
- Type discriminators on `ai_task_instance` via a `type` column have replaced the legacy `is_archetype` flag for distinguishing service tickets.
- No EAV_ARCHITECTURE documentation file was found in the docs directory despite being referenced in migration 158 comments.

## Status changes implied

No status changes are implied by this session. The session was read-only schema and code investigation with no implementation work performed.

