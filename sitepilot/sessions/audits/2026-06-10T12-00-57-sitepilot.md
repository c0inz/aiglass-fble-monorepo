---
schema_version: 1
audit_id: 2026-06-10T12-00-57-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/workflows/wf_d588297d-c49/agent-a11888afdfa3c799f.jsonl
  session_started: '2026-06-10T21:02:50.934Z'
  session_ended: '2026-06-10T21:05:04.693Z'
  session_size_bytes: 186055
  truncated: true
---

## Decisions made

The Alt1 design — "FULL ORNAMENT" — was selected as the successor to the tabled Plan v1 (Fact Harness). Design 1 (FULL ORNAMENT) was awarded the win by all three judges with scores of 8, 8.5, and 8 respectively; Design 2 (WAINSCOT) scored 7.5, 7.5, and 7. The decision was based primarily on database-level write enforcement (trigger+GUC versus code discipline), better DDL budget management with an overflow slot, and correct DATE handling via per-row companion columns.

Plan v1 (the Fact Harness) remains tabled as of 2026-06-11 per John's prior rejection. The projection-of-rows model is not being revived.

The following architectural decisions are recorded within the session:

- `ai_venture_ext` (1:1 ornament table, PK→FK on `ai_venture`) is the chosen storage for all scalar project/account properties, rather than widening `ai_venture` directly.
- A separate `am_promoted_fact` table will hold promoted workflow-fact declarations (adopted from Design 2 as a judge graft), rather than overloading `am_field_spec`'s existing task_virtual binding columns.
- `storage='native'` rows will be seeded in `am_field_spec` for the ~15 standard `ai_venture` facts so that one registry drives the entire properties tab and the `def_group_label IS DISTINCT FROM 'Standard'` magic string at projects.js:929 is eliminated structurally.
- Column-name minting convention: `p_<slug>_<spec_id>` for properties and `f_<fact_key>` for promoted facts; these physical names never appear on the wire. API keys use `internal_name` for properties and a suffix-free, cross-tenant-identical `fact_key` for facts.
- Per-column optimistic concurrency: If-Match tokens scoped to the latest `ai_ext_audit` id for that key, not row-level `touched_at`, to avoid false conflicts on unrelated property edits.
- `GET /fact-defs` will include `fed_by` bindings and coverage counts.
- DATE columns use `timestamptz` plus a companion `__all_day boolean` column because `is_all_day` is per-entry, not per-spec. This was verified on prod: spec 9600 has 312 valued rows with both variants.
- No per-option CHECK constraints; option edits must never require DDL.
- Deactivated option keys are kept in the registry forever so historical values always render.
- Phase 3 deletion of the 189,244 venture-anchored property entries requires John's explicit per-action approval and a tested restore drill before the DELETE is executed. The exact delete predicate is: `venture_ref IS NOT NULL AND step_instance_ref IS NULL AND spec.storage='column'`.
- The Phase 4 digest cutover is gated on a 1-week prod shadow-compare (new column answer vs incumbent ILIKE answer, every diff explained) before the digest reads from columns.
- Until workflow-cascade hooks are verified in Phase 4, promoted facts carry an explicit nightly-freshness SLA and automation conditions on fact columns are forbidden.
- `lib/workflow.js:337/443` cascades, `visibility-engine:241`, `inbox:163`, and `service-tickets:530/873/925` write `ai_task_instance` directly, not through `lib/task-virtual-entry.js`. Phase 4 must add fact hooks inside those cascade functions; the design's "no new hook discipline" premise is corrected.
- The `resolveFact()` function must be shared by the hook and the nightly recompute, with a unit test asserting they produce identical output (one-algorithm invariant).
- Budget gates: warn at 800 live attnums, hard-refuse at 1,200; archive ≠ DROP; a `column_table` overflow slot is baked into the registry schema for a future `ai_venture_ext2`.
- Reconcile heartbeat surfaced in both `/healthz` and a daily-digest line in addition to monitoring.
- Rule-JSON label→spec_id migration must emit a loud report listing every unmatched or ambiguous automation rule for human confirmation; nothing is silently disabled.
- Form/WO values (`ai_field_entry` step/task-anchored rows with frozen `def_*`) remain unchanged through all phases. The mig-158 def_* freeze is untouched.
- `instantiateProjectProperties` and the 177k eager-instantiation padding rows are retired at Phase 3.

## Open questions

- Phase 5 (form→column bridge via `venture_column_ref`) requires a declared `collapse_rule` per binding rather than defaulting to last-save-wins. The specific UI and workflow for this declaration has not been fully specified within the session.
- Phase 6 (optional, deferred): retiring 3,235 task_virtual mirrors by reading `ai_task_instance` natively is explicitly out of V1 scope; no timeline or trigger for this was discussed.
- Prod monitoring (`/healthz` alerts) remains one of the two stated unbuilt go-live must-haves at the time of the session. No timeline or assignee was specified.
- `venture_column_ref` is currently NULL on every BRI0 row; the session does not specify how or when the form-builder binding UI for Phase 5 will be built or what the declaration UX looks like.
- The rule-JSON label→spec_id migration will produce an unmatched-rule report for human confirmation. Who reviews and approves the report, and at what point in Phase 2, is not specified.
- No explicit effort estimate was given for Phase 3 beyond "3–4 days + approval gate." The approval process (how John provides per-action approval in practice) is not specified.

## People mentioned

John (owner; rejected Plan v1; provided the verbatim Alt1 direction; must provide explicit per-action approval before Phase 3 delete).

## Dates and deadlines referenced

- 2026-06-10: DB numbers read from prod AIEPC_BRI0 (read-only psql via tunnel at 127.0.0.1:15433).
- 2026-06-11: Plan v1 (Fact Harness) tabled on this date per John's rejection.
- No other concrete deadlines or ship dates were stated in the session.

## Thoughts not yet in the canvas

- The session explicitly acknowledges that phase-chip/kanban identity (evidence sites #17–20) requires mig-164-style `internal_name` on stage/WO blueprints and is scheduled in Phase 4. This is distinct from the property spec minting in Phase 0 and represents a separate deliverable that must appear in the Phase 4 scope.
- The session notes that 94% of BRI0's 189,244 property entry rows (approximately 177,787 rows) are empty eager-instantiation padding with no data value. The retirement of these rows at Phase 3 is a significant reduction in table size with no information loss, but the operational impact (backup size, pg_dump timing) is not assessed in the session.
- The backfill in Phase 1 copies 11,457 valued entries. The session states the backfill is idempotent and verified by shadow-read diff, but does not specify what tooling or process handles disagreements identified during the ≥1-week parity check before Phase 2.
- The session identifies that the Coperniq sync (`scripts/sync-coperniq.js`) is retired for BRI0 but that the loaders remain templates for imports. The disposition of Coperniq sync for tenants other than BRI0 (e.g., SND0, MAS1) is not addressed.
- The session notes that `routes/agent-api/*` currently has zero `ai_field_entry` references and is treated as greenfield. The timeline or prioritization for building out the full Agent API surface is not specified.
- The session identifies that only 83 of 695 BRI0 property specs currently have `internal_name`. Phase 0 mints the other 612. The minting script and validation criteria are described conceptually but not yet implemented.
- The session contains a full risk register (§7) and go/no-go conditions that should be tracked as standing items: the P2 gate (≥1 week zero parity diffs), the P3 gate (verified restore drill + approval), the P4 gate (1-week shadow-compare with every diff explained), and the overall NO-GO condition (P1 backfill cannot reach zero-diff).

## Status changes implied

No track/milestone slugs from the canvas were referenced in this session. The session describes a newly synthesized design plan (Alt1 / FULL ORNAMENT) moving from proposed to a detailed implementation plan with phasing and risk register, but no existing canvas milestones map to this work as described. The following status changes are implied for any canvas items that do exist or are created from this work:

- Any canvas item tracking Plan v1 / Fact Harness should remain `tabled` (confirmed in session).
- Any canvas item tracking the Alt1 / FULL ORNAMENT design moves from `proposed` to `ready for implementation` based on judge consensus and the synthesized plan being finalized in this session.

