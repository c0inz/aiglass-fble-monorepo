---
schema_version: 1
audit_id: 2026-06-10T12-01-21-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-06-10T12:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6.jsonl
  session_started: '2026-05-06T17:15:54.507Z'
  session_ended: '2026-06-10T23:36:05.656Z'
  session_size_bytes: 334202012
  truncated: true
---

## Decisions made

The operator decided to wipe and reload all three target accounts (615611, 693903, 694215) into BRI0 production using the canonical Coperniq extractor (`scripts/parity-test/02-load-from-coperniq.js`) rather than any ad hoc method.

The operator confirmed that the master test suite provisions its own isolated tenant schema (`AIEPC_<slug>`) and does not touch BRI0; it may be run as soon as the UI is functional, in parallel with the batch export driver.

The operator confirmed that all 579-minus-3 remaining accounts should be pulled in batches of 25 at 8-minute intervals using the existing batch driver.

Migration 123 was applied (not reversed): all 70 ON DELETE CASCADE foreign keys in BRI0 and MAS0 were converted to NO ACTION.

The operator confirmed that iterating on UI route patches via a single batch deploy (all three browse-blocking files at once) is acceptable given no real users are on the app.

The operator confirmed the 16 unpushed local commits should be pushed to `origin/main`.

The Fact Harness (Plan v1, projection-based, `ai_fact_point` copy table) was tabled by the operator. The operator stated the design is fundamentally unsatisfactory and requires a do-over; nothing was built and the `docs/fact-harness-plan` branch docs were marked TABLED — do not build.

An alternative design (Alt1, "Full Ornament") was commissioned and completed: per-tenant `ai_venture_ext` ornament table with real typed columns, `am_field_spec` promoted to a column registry, workflow-derived facts as machine-written recomputable columns, and forms/WOs remaining as EAV rows permanently. The operator received the Alt1 plan at `docs/ALT1_ORNAMENT_PLAN.md` on the `docs/fact-harness-plan` branch but had not approved it for implementation by session end.

The pin concept from Plan v1 was rejected by the operator as unnecessarily weak. The agent proposed replacing pins with writable facts that route writes back into the original capture location (fact write inverts the read path through the resolver). The operator did not issue a final approval on this; the session ended before a decision was recorded.

Current-phase semantics was locked as structural: minimum-rank `IN_PROGRESS` stage. The anchor identifies the installation stage; it does not redefine "current phase."

## Open questions

Whether to proceed with Full Ornament (Alt1) or the Wainscot variant (the more conservative hybrid that shares P0–P3 with Alt1 but defers P4 workflow hooks) was not resolved.

The catalog seed list (~35 fact keys with planned/actual splits) was not produced; the agent stated it requires domain input from the operator before any implementation begins.

Pin policy (defer entirely, writable-into-capture, or another approach) was not finalized.

Whether the API PATCH for column-backed facts should write capture directly or follow a uniform pin/override semantics was not resolved.

The operator's broader architectural do-over direction (standard tables per tenant + custom column ornament for all tenant fields) was stated as intention but not scoped or approved for work to begin.

Whether the EAV forms slice (issue #196) and the deferred internal_name/blueprint-meta discussion should remain frozen pending the do-over decision was not explicitly confirmed; the agent stated it would treat them as frozen unless instructed otherwise.

## People mentioned

Tom Davenport (referenced as owner/created-by on account #243 in Coperniq data the operator pasted).
Isaac Rosa (referenced as owner on account #544 in the pasted Coperniq data).
Keaton Garner (referenced as owner on account #306 in the pasted Coperniq data).

## Dates and deadlines referenced

2026-05-04: date the Coperniq OAuth token was last refreshed; valid through 2026-05-18 23:49 UTC (noted as within the ~3-hour batch export window).

2026-06-06: gate date for migration 103 (final `_old` table drop); noted in the workplan as a future constraint.

2026-05-06: session start date; all route patching, batch driver kickoff, BRI0 reload, and architecture review occurred within this session.

2026-06-10: session end date (as recorded in session metadata).

2026-06-09: date of DATA_SHAPE_AND_AGENT_API_DESIGN.md §12 decision (field key as `(group,label)`, BRI0 remediation applied — 11 field renames, 12 groups named).

## Thoughts not yet in the canvas

The agent identified that the actual blast radius of the route refactor is 19 files referencing `ai_step_instance` and 14 referencing `ai_work_ticket_old` — significantly larger than the workplan's "3 browse-blocking files" characterization. All were patched in this session.

`ai_remark.work_ticket_ref` does not exist post-migration; this was discovered during the sweep and patched.

The loader (`scripts/load-one-account.js`) was missing FK hydration for `process_ref`, `active_stage_ref`, `serial_number`, owner, manager, and address fields on child projects. These were patched and all three accounts reloaded.

`scripts/wipe-acct.js` attempted to delete `am_field_spec` / `am_field_arrangement` rows shared across accounts (not scoped per-venture), which failed after the CASCADE-to-NO-ACTION migration. The wipe script was patched to leave those tables alone (the loader UPSERTs them).

A false-positive in the headless crawler (`scripts/crawl-bri0.js`) was found: it was matching `ReferenceError:` text inside inline JS code comments. The crawler was tightened to avoid this.

The headless crawl (179 step detail pages across 4 projects) confirmed all pages render clean after fixing a single remaining bug: `routes/forms.js` referenced `es.assignment_ref` on `ai_event_stream` (which has `step_instance_ref`, not `assignment_ref`). After that fix, zero findings across 179 form/WO pages.

The prod DB is on DigitalOcean private network (`10.124.0.2`), not on `sadb`; the tunnel must be established via `root@143.110.232.40`.

An `ecosystem.config.js` pm2 guard (`max_restarts: 10`, `min_uptime: 10000`) was deployed to prod to prevent a repeat of the prior 169-restart event.

A GitHub credential helper conflict was identified (a spaiglass helper shadowing the credential store) and worked around via inline credential helper on git push.

Batch account 308312 failed during the batch export driver run with a SQL syntax error in `scripts/parity-test/02-load-from-coperniq.js:486` (empty IN-clause on an account with no rows); noted as unrelated to schema work, to be re-run by hand.

The 13-agent Alt1 review identified that the per-schema backup capability does not exist — the prod backup is one whole-database dump. Building per-schema dumps was identified as a P0 ops item for Alt1.

The Alt1 review found the column-budget math was off by ~46% on the stress tenant (SND0) once DATE companion columns are counted (~993 day-one). The plan was corrected with companion-inclusive budgets, lazy minting, and an early-built overflow table.

Sync/import scripts write property rows directly; dual-write therefore needs to be moved to P1 and the rejection trigger to P2 in Alt1, to close the migration window where truth could fork.

Dropdown values in BRI0 contain ghosts (133 entries storing options that no longer exist in active specs); option-key minting from the union of live + frozen + stored values is a priced P1 item in Alt1.

Retyping a field's `data_kind` is a live UI capability today; under Alt1 this becomes DDL, with designed semantics: refuse in-place, new column + cast + archive old.

The operator noted the prior session operated with thinking disabled ("no think") and caused numerous problems; this session was started explicitly with thinking set to high.

## Status changes implied

No canvas slugs are defined in §7 of the playbook; no existing slug references can be asserted. The following status changes are implied by the session content for tracking purposes:

UI route sweep (all 17 files): moved from open/blocked to complete and deployed.

BRI0 account reload (615611, 693903, 694215): moved from partial/incorrect to complete with all FK hydration fields populated.

Coperniq batch export driver (batches 1–23): moved from not started to in progress (at batch 7–9 of 23 by session end).

Migration 103 (`_old` table drop): remains gated until 2026-06-06; no change.

Fact Harness Plan v1: moved from proposed/in-review to tabled — do not build.

Alt1 Ornament Plan: moved from not started to drafted and committed (awaiting operator approval before any implementation).

