---
schema_version: 1
audit_id: 2026-05-29T18-00-08-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-05-29T18:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/agent-ab421029c4ae31c25.jsonl
  session_started: '2026-05-28T18:03:05.045Z'
  session_ended: '2026-05-28T18:09:53.781Z'
  session_size_bytes: 673442
  truncated: false
---

## Decisions made

No decisions were made in this session. The session consisted entirely of an operator-initiated code review request with no explicit decisions recorded.

## Open questions

No data. The session was a read-only code review with no open questions raised by the operator.

## People mentioned

No people were mentioned by name in the operator input or assistant output.

## Dates and deadlines referenced

No dates or deadlines were referenced.

## Thoughts not yet in the canvas

The operator commissioned a full prioritized code review of the sitepilot codebase focusing on correctness bugs, security issues, and reuse/cleanup opportunities, covering 9 PRs shipped in the prior 24 hours (commits 1a368ee through 35314c1). The review surfaced the following findings not currently mapped to any canvas milestone or slug:

**Critical findings:**
- `lib/workflow.js:213` and `routes/service-tickets.js:519`: hardcoded tenant_ref value for a specific production tenant inserted into `ai_event_stream` on any tenant's step-status change, causing cross-tenant data pollution.
- `routes/projects.js:2387`: an authenticated user can send arbitrary HTML email via the application's transactional email account (Resend) to any recipient, with no rate-limit, recipient whitelist, or venture-membership check. Identified as a spam/phishing abuse vector.
- `middleware/permission.js:53,85,127`: `requireSettingsAccess`, `requireEntityAccess`, and `requireMetaAccess` all fail open when effective roles resolve to an empty set, bypassing every permission gate for workers with zero role rows.
- `routes/projects.js:1749` and `1839`: collaborator POST/DELETE endpoints do not assert that the target task belongs to the specified venture; cross-project task manipulation is possible. The attachment endpoint at lines 1925–1947 has a related gap.

**High-severity findings:**
- `routes/inbox.js:91`: any authenticated worker can cancel any service ticket or work order in the tenant by iterating ids; no ownership or assignee check.
- `routes/service-tickets.js:814`: assignee, priority, and date changes on tickets do not emit through `workflow.applyTaskAssigneeChange`, causing inbox, activity log, and auto-status-bump invariants to be skipped. Diverges from the `routes/projects.js` path.
- `routes/projects.js:611` and `lib/load-project-rail.js:247`: phase-pipeline subquery is duplicated with copies that have already drifted; should be a single helper.
- `lib/inbox/create-event.js:353–708`: `buildRecipientSet` and several associated functions (~250 lines total) are dead code since PR #16 replaced them with `resolveRecipients`; references in routes and docs still treat them as live.
- `routes/schedule.js:374`: CSV export does not prefix formula-triggering characters (`=`, `+`, `-`, `@`) in cell values, enabling CSV injection via user-controlled task titles.
- `routes/projects.js:39`: project provisioning runs outside a transaction; mid-flight failure leaves a partially created project and dependency graph.
- `views/inbox.ejs:457`: `groupAttrs` built by string concatenation is emitted with `<%-` (raw EJS output).
- `views/settings/notifications.ejs:30–55`: `receiver_kind` and `receiver_value` columns concatenated and emitted with `<%-`; admin-only XSS surface.

**Medium findings:**
- Worker picker query is duplicated across `routes/accounts.js:481`, `routes/agents.js:553`, `routes/schedule.js:77`, `routes/my-work.js:70`, and `lib/load-project-rail.js:218` with different filter sets; `lib/load-active-workers.js` is the canonical helper but not consistently used.
- Field-update allow-list pattern is hand-rolled in at least three routes with diverging allow-lists; a shared helper would eliminate the drift.
- `lib/workflow.js:212–223`: `ai_event_stream` always writes `event_kind='TASK'` regardless of actual entity type; downstream filtering breaks.
- `routes/inbox.js:117–318`: dual writes to legacy `ai_inbox` and new `ai_inbox_event`/`ai_inbox_recipient` tables; one should be retired.
- `migrations/155_collaborator_task_anchor.sql`: hand-fixed twice; FK firing order relative to dedup branch needs audit on cold-clone tenants.
- Phase pipeline subquery in `routes/projects.js` not yet delegated to `loadProjectRail`; copies have already drifted once.

**Low/informational findings:**
- `lib/concurrency.js:30`: `table` argument interpolated directly into SQL; all current callers are static but the pattern is a footgun.
- `routes/forms.js:179`: dangerous-extension filter blocks `.js`/`.jar`/`.exe` but permits `.html`, `.svg`, `.xhtml`, enabling stored XSS via uploaded SVG served through the file proxy.
- `middleware/tenant.js:66`: `search_path` schema pattern `/^AIEPC_[A-Z0-9]{4}$/` will reject future tenants with lowercase or longer slugs; constraint is undocumented at signup.
- `routes/projects.js:2241–2308`: serial number assignment uses `SELECT COALESCE(MAX(serial_number)+1)` inside a loop — both an N+1 query and a race condition under contention; should use a database sequence.

**Systemic patterns identified:**
- `ai_event_stream` has multiple writers that bypass the create-event resolver; should route through a single tenant-resolving helper or fold into the inbox emit.
- "Which workers can I pick?" is expressed as four or more different SQL queries across routes; `loadActiveWorkers` is not enforced as the single source of truth.
- Permission gates fail open at empty-roles branch; real authorization is scattered inside route SQL with no single auditable chokepoint.
- Mutations to `ai_task_instance` are inconsistently routed: `routes/projects.js` goes through `lib/workflow`, `routes/service-tickets.js` writes raw SQL, `routes/schedule.js` and `routes/my-work.js` have no mutation path; PR #16's centralization is described as half-fulfilled.

## Status changes implied

No data. The session was a read-only code review; no milestone status changes were explicitly implied or stated.

