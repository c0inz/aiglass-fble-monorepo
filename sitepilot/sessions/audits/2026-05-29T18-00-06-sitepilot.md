---
schema_version: 1
audit_id: 2026-05-29T18-00-06-sitepilot
product: sitepilot
generated_by: claude-sonnet-4-6
generated_at: '2026-05-29T18:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/6cf5624f-bb8d-49e0-b24a-6b84b5f309b6/subagents/agent-addc374c529f92750.jsonl
  session_started: '2026-05-26T21:08:56.963Z'
  session_ended: '2026-05-26T21:13:47.273Z'
  session_size_bytes: 390340
  truncated: false
---

## Decisions made

No final implementation decisions were made. The session was scoped explicitly to design analysis only — the operator instructed the agent not to implement or edit any code. The deliverable was a written design comparison, not a build decision.

The agent produced a design comparison recommending Option B (user-settable display density via rem migration and a Compact/Default/Comfortable toggle) layered on a minimal subset of Option A's scroll-container cleanup. This is a recommendation for the operator to evaluate, not a confirmed decision.

## Open questions

1. Density default — should the html root font-size default to today's Compact feel (14px) or a new Default (16px)? This is a product call with audience tradeoffs.
2. Density scope — user-only, tenant-sets-default with user override, or tenant-locks? The existing theme_preference pattern supports (b).
3. Should density scaling include spacing/padding tokens (--space-*) in addition to typography, or typography only?
4. Inline-style sweep staging — all four templates in one PR, or staged (work-header first, then form, then work-order, then detail)?
5. Is removing the .comments-rail max-height:calc(100vh - 100px) independent scrolling an acceptable UX tradeoff for the zoom-sync fix?
6. Is Option C (full-page URL navigation, reversing the FrameNav modeAware embed) on the table at all, or is preserving the workspace shell during step drill-in a hard requirement?
7. Mobile density baseline — should mobile always use Comfortable for touch targets, or honor the user's saved density preference?

## People mentioned

No individual people were named or referenced in the session inputs.

## Dates and deadlines referenced

No dates or deadlines were referenced in the session inputs.

## Thoughts not yet in the canvas

The session surfaced a concrete root-cause analysis and five named design options for the /projects desktop density and browser-zoom desync problems. Key technical findings not yet captured elsewhere:

- The design system declares font sizes as fixed px tokens (--font-size-xs: 11px through --font-size-xl: 20px) per docs/DESIGN_SYSTEM.md §1 "data density first" — the small feel is deliberate product policy, not an accident.
- Roughly 60-80 inline font-size:Npx occurrences exist across detail.ejs, work-order.ejs, form.ejs, and work-header.ejs — many below the token floor (9px label, 10px badge, 11px meta).
- No html { font-size } rule exists; body is set to var(--font-size-base) = 14px; the rem unit cascades from the UA default (16px), not from the design system.
- The zoom desync root cause is three nested calc(100vh - N) scroll containers: .split-list, .split-detail (list.ejs), and .comments-rail inside each of detail.ejs, work-order.ejs, and form.ejs. The .comments-rail uses position:sticky; top:80px scoped to .split-detail, not the document — causing the rail and form body to appear to scale independently under browser zoom.
- The inner 1fr 340px grid-template-columns is px-fixed but scales with browser zoom; the desync comes from the sticky comments rail ceiling, not the grid.
- No transform: scale or zoom CSS is present anywhere in the codebase.
- Option D (iframe embed) was already rejected in docs/PLAN_WO_FORM_EMBED_DESKTOP.md §3 Option C for documented reasons (two doc lifecycles, two history stacks, CSP, postMessage choreography).
- Option C (full-page navigation) would reverse the workspace-shell continuity decision codified in PLAN_WO_FORM_EMBED_DESKTOP.md.
- The recommendation to pair Option B with a minimal Option A scroll-container cleanup (dropping only .comments-rail overflow-y:auto and max-height, keeping .split-detail outer scroller) is the agent's preferred path. Files identified for change: design-system.css, layout.ejs, work-header.ejs, detail.ejs, work-order.ejs, form.ejs, routes/me.js, routes/auth.js, views/me/account.ejs, a new migration, and mobile.css simplification.

## Status changes implied

No status changes are implied. The session was analysis-only; no milestone work was started or completed.

