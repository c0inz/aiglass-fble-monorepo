---
schema_version: 1
audit_id: 2026-05-20T16-10-10-trendzion
product: trendzion
generated_by: claude-sonnet-4-6
generated_at: '2026-05-20T16:08:00Z'
sources_summarized:
- kind: claude_session
  path: /home/readystack/.claude/projects/-home-readystack/601eec4b-b34c-44f9-b6d4-1bd4ef9f3ae1.jsonl
  session_started: '2026-04-18T02:53:41.208Z'
  session_ended: '2026-05-19T20:59:10.470Z'
  session_size_bytes: 122580369
  truncated: true
---

# Session 2026-04-18 — Corpus v1 data ingestion, provenance canon, UI fixes, X OAuth

## Decisions made

The operator decided to include JS—History (Pearl of Great Price) in the corpus because Joseph Smith authored it, reversing a tentative suggestion to remove it.

Doctrine and Covenants was confirmed as a first-class v1 canonical source (`source='d_and_c'`), requiring 138 sections plus OD-1 and OD-2 with correct per-section author attribution. D&C §134 is attributed to Oliver Cowdery, §135 to John Taylor, §136 to Brigham Young, §138 to Joseph F. Smith, OD-1 to Wilford Woodruff, and OD-2 to Spencer W. Kimball.

JSP Histories were pushed to v2; JSP Journals were confirmed complete for v1 (10 records already loaded).

Ensign and New Era are treated as two separate sources, not combined; `source='ensign'` covers 1971–2020, and Liahona-era content (2021+) is a v2 item. All articles are loaded regardless of speaker — no apostle-only filter applied because the raw data was already scraped.

Duplicate conference reprints are flagged with `is_duplicate=TRUE` and a `duplicate_of` pointer to the cojc canonical row; both records are kept ("data is cheap").

The provenance canon was extended: no NULL fields are permitted anywhere; sentinel values `'n/a'` (text) and `-1` (integer) are used for genuinely inapplicable fields, and every sentinel row requires a `notes` entry justifying it. A `delivered_on_precision` field (values: day, month, range, year, unknown) was added to the schema.

A shared canon enforcement module (`agents/scripts/lib/canon.py`) and a post-load audit script (`agents/scripts/audit_canon.py`) were built and all existing loaders were retrofitted. All 12 sources passed the post-canon audit with zero violations.

For Times and Seasons, the two broken ad-hoc rows were deleted. Both issue-level and article-level segmentation will be done together in v1 (not deferred) because the 1839–1846 period was judged too historically valuable to defer to v2. Legacy on-disk files were deleted before re-fetching from archive.org. The LLM subagent OCR correction pattern (from `research/strategies/ocr-correction.md`) is the required approach.

cojc 2024-10 was rescraped to restore raw/jsonl/sadb three-way parity. The 110-row raw-vs-jsonl discrepancy was resolved: those files are `_index.json` scraper metadata, not lost article rows. Final reconciled count: 4,266 article raw = 4,266 jsonl = 4,266 sadb.

For the X integration, OAuth 1.0a via `requests_oauthlib` remains the active auth method for posting; OAuth 2.0 PKCE is provisioned but unused. The canonical X app for trendzion is `fable_software_trendziondotcom` in `~/credentials/x.json`. The personal @JohnDavenport handle was authorized against this app and tokens were synced back to the agent credentials file; the tokens were verified live against `GET /2/users/me`.

Access tokens previously stored in `~/credentials/x-developer.json` for what was believed to be @JohnDavenport were confirmed dead (401 Unauthorized); those credentials are no longer functional.

For the UI/frontend work (earlier in the session, prior to compaction): og:image cards were fixed to 1200x630 native resolution (deviceScaleFactor:1 in Playwright); `/w/deseret-2` alias with a 308 redirect was created to bypass X's unfurl cache on the old URL; a `scripts/dev.sh` wrangler dev script was created using port 8788 as the canonical local dev port; API CORS was extended to include `localhost:8788`; a 12-assertion Playwright regression suite was built and verified passing both locally and against prod. These changes were not committed to git before the session ended.

## Open questions

Whether the testing infrastructure changes (scripts/dev.sh, playwright.config.js, tests/e2e/fixtures.js, tests/e2e/browser/regressions.spec.js, package.json, api/trendzion_api.py) should be committed to the `og-card-deseret` branch and pushed — the operator exited without answering this question.

Whether the @JohnDavenport authorization on the `fable_software_trendziondotcom` X app will be used and for what purpose — no action was taken after confirming the tokens are live.

Times and Seasons subagents for Vols 1, 3, 5, and 6 returned results by the time the session snapshot was cut; a normalizer and loader for T&S have not yet been written; that work is pending.

Whether any other X handles beyond @TrendZion and @JohnDavenport should be authorized against the trendzion app.

## People mentioned

Joseph Smith (corpus author, subject of multiple sources). Oliver Cowdery (D&C §134 author). John Taylor (D&C §135 author; also author of the Martyrdom account in T&S Vol. 5). Brigham Young (D&C §136 author; also presided at Oct 1844 conference in T&S Vol. 5). Joseph F. Smith (D&C §138 author). Wilford Woodruff (OD-1 author). Spencer W. Kimball (OD-2 author). LaJean Carruth Pitman (transcription source, not yet downloaded). Sidney Rigdon (Trial of Elder Rigdon in T&S Vol. 5). Don Carlos Smith (mentioned in T&S Vol. 2 mourning context). Gov. Ford (Illinois extradition documents in T&S Vol. 4).

## Dates and deadlines referenced

2026-04-18: session date; all corpus work, schema migrations, and loaders executed on this date.

2026-04-28: date the `fable_software_trendziondotcom` X app was created and @TrendZion user tokens were originally authorized.

1971–2020: Ensign raw data coverage window.

1839–1846: Times and Seasons publication run, the full target for v1 ingestion.

1828–1844: JSP Documents series year range (1,159 records, confirmed loaded and complete).

No hard deadlines for v1 close were stated in the session.

## Thoughts not yet in the canvas

The x_user_id callback guard in `api/trendzion_api.py` pre-declares allowed user IDs to prevent token overwrites. To add any new X handle in the future, the allow-list logic may need a one-line change. This constraint was noted but no milestone covers X multi-account management.

The apostle books page on `static/research.html` is backed by `static/data/apostle_books.json` (232 books cataloged; 70 public-domain; 54 with URLs) but every row shows `in_corpus=false` even though 22 books are loaded in sadb. Fixing that staleness was logged as a v1 bug item in the WORKPLAN but does not appear to have been executed this session.

T&S Vol. 4 subagent noted that the DjVuTXT column-interleave artifact (two newspaper columns mashed line-by-line) cannot be resolved with regex and requires re-OCR with a structure-aware or vision LLM. This is a known quality limitation not captured in any existing milestone.

T&S Vol. 2 subagent noted that the state-agents listing at the end of each issue inflates article counts because the title detector splits on each state header. This is a known segmentation artifact.

T&S Vol. 5 subagent identified the King Follett Discourse (`1844-08-15_vol5_iss15_art04.json`, attributed to Joseph Smith), the Martyrdom account (John Taylor, `1844-07-01_vol5_iss12_art01.json`), the Trial of Elder Rigdon (Sidney Rigdon across three issues), and a Jun 15, 1844 issue gap caused by the Martyrdom crisis. These are high-value individual articles that the operator had called out as v2 article-level promotion candidates; no milestone exists for that promotion work yet.

The `scrape_ensign_fast.py` and `scrape_new_era.py` scripts that produced the raw data were never committed to the repo. Only the raw output survives. This was noted as a gap but not resolved this session.

## Status changes implied

The following sources moved from not loaded to loaded in sadb during this session: `source='d_and_c'` (140 rows / 105,884 words), `source='ensign'` (3,421 rows / 7.1M words; 1,732 marked duplicate of cojc), `source='new_era'` (753 canonical rows / 980,212 words; 6 marked duplicate of cojc).

The cojc source moved from a three-way discrepancy state to a fully reconciled state (4,266 raw = 4,266 jsonl = 4,266 sadb).

The provenance schema moved from allowing NULLs to enforcing NOT NULL with sentinel-requires-notes constraint; this is a breaking schema change applied via migration `20260418_provenance_canon.sql`.

The corpus enforcement stack moved from prose-only documentation to active code enforcement via `lib/canon.py` (loader-time) and `audit_canon.py` (post-load sadb audit).

Times and Seasons moved from 2 broken ad-hoc rows in sadb to 0 rows (deleted), with fresh raw fetched from archive.org and OCR+segmentation subagents completed for Vols 2, 4, and 5 (Vols 1, 3, and 6 completed but loader not yet written).

@JohnDavenport X OAuth tokens moved from absent/dead to live and synced in `~/credentials/x.json`.

