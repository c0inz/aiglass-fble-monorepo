---
schema_version: 1
audit_id: 2026-05-29T18-00-03-trendzion
product: trendzion
generated_by: claude-sonnet-4-6
generated_at: '2026-05-29T18:00:00Z'
sources_summarized:
- kind: claude_cli
  path: /home/readystack/.claude/projects/-home-readystack/601eec4b-b34c-44f9-b6d4-1bd4ef9f3ae1.jsonl
  session_started: '2026-04-18T02:53:41.208Z'
  session_ended: '2026-05-30T03:57:29.074Z'
  session_size_bytes: 123230601
  truncated: true
---

## Decisions made

The user decided to keep the Joseph Smith—History row in the corpus because the leader wrote it; it was not deleted from sadb.

Doctrine and Covenants was added as a first-class canonical source (`source='d_and_c'`) with 138 sections plus OD-1 and OD-2, each carrying correct per-section author attribution (Oliver Cowdery for §134, John Taylor for §135, Brigham Young for §136, Joseph F. Smith for §138, Wilford Woodruff for OD-1, Spencer W. Kimball for OD-2).

The Times and Seasons pipeline was decided to handle both issue-level and article-level segmentation in v1; the user rejected deferring article-level to v2 because the time period is too valuable to push off.

Ensign and New Era non-apostle articles were retained rather than excluded on the grounds that data is cheap and excluding non-apostle speakers was not worth the effort given the data was already scraped.

Ensign and New Era were kept as two separate sources rather than combined; Liahona (2021+) was assigned to v2.

Both duplicate records (Ensign reprint and cojc original) are kept, with the Ensign version flagged `is_duplicate` pointing to the cojc canonical. This is the standing dedup policy.

For the X developer integration, the canonical app is `fable_software_trendziondotcom` using OAuth 1.0a (chosen for media-upload support). OAuth 2.0 PKCE is provisioned but unused.

@JohnDavenport was authorized into the X integration by the user completing the OAuth flow; tokens were synced from the API host to local credentials.

The `og-card-deseret` branch was rebase-merged to main via PR #1. CF Pages deploy was confirmed not needed because all artifacts were already on prod from a prior session.

Local stale `og-card-deseret` branch was deleted. A duplicate "Testimony"/"testimoni" slug issue was filed as GitHub issue #2 rather than fixed in the merge to avoid scope creep.

"Urim and Thummim" (`urim-and-thummim`) was added to the reading library with strict literal match pattern `["Urim and Thummim"]`. "Seer" (`seer`) was added with patterns `["seer", "seers"]`. "Second Coming" (`second-coming`) was added to PR #3.

A durable canon enforcement stack was built: `agents/scripts/lib/canon.py` (loader-time validator), `agents/scripts/audit_canon.py` (sadb-side audit), a DB check constraint `talks_sentinels_require_notes`, and a memory file `project_lds_provenance_canon.md`.

All NULL fields in sadb were replaced with sentinel values (`'n/a'`, `-1`, `'unknown'`) plus a mandatory `notes` justification column; NOT NULL constraints were added to all text columns.

The sadb SSH access pattern from rsagent4 was confirmed and saved to memory: rsagent4 → SSH to bombadil → SSH to sadb as dbadmin, with `ssh sadb` working directly via ProxyJump; the working query command is `ssh sadb 'sudo -n -u postgres psql -d readystack -c "..."'`.

`build_og_image.mjs` was modified to support an `OG_BASE_URL` environment variable override so OG cards can be baked against a local `wrangler pages dev` server rather than requiring a two-phase prod deploy.

## Open questions

Whether the GrandGallery droplet has automated DigitalOcean backups or snapshots enabled was raised but not answered before the session ended.

Whether to fix the pre-existing `text-transform: capitalize` CSS bug on `.entry-title` that causes multi-word term OG cards to show incorrect capitalization (e.g., "Urim And Thummim", "Book Of Mormon") was raised but not resolved.

Whether to bump the `waitForFunction` timeout in regressions.spec.js test 8 from 10s to 15s to absorb occasional Cloudflare cold-edge flakes was offered but not approved.

The six auth security gaps identified in the API were presented for the user's decision: no auth-specific throttling or failed-login lockout, inconsistent password minimum length (6 for signup, 8 for reset), no email verification on signup, session token stored in localStorage, in-process rate-limit state, and the database password hard-coded in source. No action was taken.

GitHub issue #2 (duplicate "Testimony"/"testimoni" slug in index.json) is open with no resolution date.

Whether to merge PR #3 (Urim and Thummim, Seer, Second Coming) and deploy via `wrangler pages deploy` was not answered before the session ended.

Whether to commit and push the testing infrastructure changes to the og-card-deseret branch (from the prior session ending with /exit) was never answered; those files were ultimately included in the merge commit.

## People mentioned

No individuals other than the operator are mentioned by name in the content of this session. Historical figures referenced as corpus subjects include Joseph Smith, John Taylor, Oliver Cowdery, Brigham Young, Joseph F. Smith, Wilford Woodruff, Spencer W. Kimball, Sidney Rigdon, and Hyrum Smith.

## Dates and deadlines referenced

2026-04-18: date of corpus work session (T&S scrape, D&C load, schema migration, Ensign/New Era load).

2026-04-20: noted in build_aggregates.py as the date a strict-literal pattern note was added for "Gathering of Israel" (referenced as existing convention).

2026-04-26: Google OAuth added to the API, replacing old X-OAuth for user sign-in (noted in migration history).

2026-04-28: `fable_software_trendziondotcom` app created; @TrendZion user token authorized; `x-developer.json` legacy file deprecated.

2026-05-04: last commit on origin/main before the og-card-deseret branch left it.

2026-05-05: first commit on og-card-deseret branch pushed; branch tip commit.

2026-05-09: @JohnDavenport X authorization completed (x_user_id 1736982014254010368, authorized 2026-05-09T14:49:36Z).

No future deadlines were stated.

## Thoughts not yet in the canvas

The T&S Vol 4 subagent noted that the DjVuTXT interleaves two newspaper columns line-by-line, which regex cannot unscramble; full resolution requires re-OCR from the image (vision LLM or structure-aware OCR). This is a known quality limitation not yet tracked.

The T&S Vol 5 subagent confirmed the Jun 15, 1844 issue was skipped due to the Martyrdom crisis; this is a legitimate gap in the run, not a data loss.

The T&S Vol 2 subagent noted that the all-caps title detector splits "agents-by-state" sub-sections (ILLINOIS., PENNSYLVANIA., OHIO., etc.) into separate articles, inflating article counts; this is a known limitation.

The trendzion user/auth Postgres (`trendzion` database, `social` schema) lives entirely on the GrandGallery droplet co-resident with the API, with no block-storage volume and no confirmed encryption at rest. This is an infrastructure note not yet in any canvas.

The `wrangler.toml` in the trendzion repo uses a project name `trendzion` and CF Pages is manually deployed (not git-integrated); this architectural note is not in any tracked milestone.

The `testimoni` vs `testimony` duplicate in index.json was confirmed pre-existing on main, caused by two pipelines (`build_aggregates.py:446` stemming to `testimoni`, `rank_data_driven.py:622` keeping surface form `testimony`) producing conflicting entries. Filed as GitHub issue #2.

The Op-sec observation that the trendzion API's DB password is hard-coded in source (`trendzion_api.py:40`) was surfaced. This is not in any existing canvas item.

The T&S Vol 5 subagent used regex-only OCR correction rather than the LLM subagent pattern because LLM subagent dispatch was unavailable within that subagent context. This is a quality caveat for the T&S corpus.

The T&S Vol 4 subagent noted Vol 4 iss 24 art 16 ("JOHN TAYLOR") and art 17 appear to be masthead noise rather than real articles. No remediation was tracked.

## Status changes implied

Corpus/d-and-c: planned → complete (140 rows loaded, zero-blank audit passed, canon-validated).

Corpus/ensign-apostle: planned → complete (3,421 docs loaded, 1,732 conference-reprint dupes flagged, canon-validated).

Corpus/new-era-apostle: planned → complete (753 canonical docs loaded, dedup against cojc applied, canon-validated).

Corpus/cojc: reconciliation complete; three-way raw/jsonl/sadb parity confirmed at 4,266 rows after rescrape of 2024-10 session.

Corpus/jsp-documents: confirmed complete for the Documents series (1,159 rows); JS Journals (10 rows) confirmed v1-complete.

Corpus/times-and-seasons: broken ad-hoc load (2 rows) deleted; six parallel OCR+segment subagents launched covering all 6 volumes; at least Vols 2, 4, and 5 completed and reported during the session. Full loader not yet written or applied to sadb as of session end.

Corpus/scripture-js-history: retained (not deleted); decision made that leader-authored content stays regardless of scripture classification.

Schema/provenance-canon: new enforcement stack shipped (canon.py, audit_canon.py, DB constraint, memory entry); all 12 sources passed zero-violation audit post-migration.

Trendzion/branch og-card-deseret: merged to main via PR #1 (rebase-merge, linear history preserved); local branch deleted.

Trendzion/terms: PR #3 open on branch add-terms-urim-seer covering three new library terms (Urim and Thummim, Seer, Second Coming); not yet merged or deployed.

X-integration/@JohnDavenport: authorized into fable_software_trendziondotcom app; tokens synced from API host to local credentials.

X-integration/legacy-credentials: x-developer.json moved to credentials/archive/ as deprecated-2026-04-28.

