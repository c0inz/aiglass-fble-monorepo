---
schema_version: 1
audit_id: 2026-05-20T16-09-10-knocklist
product: knocklist
generated_by: claude-sonnet-4-6
generated_at: '2026-05-20T16:08:00Z'
sources_summarized:
- kind: claude_session
  path: /home/readystack/.claude/projects/-home-readystack/f344c351-bf65-4ed3-9ab0-0f7b273cabdd.jsonl
  session_started: '2026-05-06T09:54:41.079Z'
  session_ended: '2026-05-12T14:37:38.900Z'
  session_size_bytes: 40937580
  truncated: true
---

# Session 2026-05-06 to 2026-05-12 — Knocklist edge PWA build, BASS recon, Sunshift, lifeparse

## Decisions made

The operator decided to build Knocklist as an edge-native door-to-door canvassing PWA deployed entirely on Cloudflare (Workers, D1, KV, Pages, Queues). The UI uses a Linear/Vercel visual style with Lucide icons, not the amber/navy theme from the solarknock fork. The pin disposition status called "follow_up" was renamed to "call_back" everywhere in the UI.

BASS integration was shelved. The operator confirmed that the BASS wizard after address confirmation requires customer-side interactions (disclosures sent to their phone, credit checks, installation scheduling) that cannot be automated headlessly. The decision was made to leave BASS as a separate manual step for sales reps rather than integrate it into Knocklist directly.

The operator decided the address seeding strategy for territories should combine OpenAddresses bulk data with Smarty US Street API for cleaning and validation, rather than using Smarty Reverse Geocoding alone.

A GitHub repository named `sunshift` was created under the c0inz organization as a fast frontend for BASS order entry on Cloudflare. The operator approved the Sunshift architecture and instructed the agent to complete all five deployment steps.

A GitHub repository named `lifeparse` was created under c0inz organization for a RealWear HMT-1 daily visual lifelog system.

Smarty Autocomplete Pro and US Street API credentials were configured as Cloudflare Worker secrets. A Smarty auth-id and auth-token were provided by the operator and set on both the worker and queue-consumer deployments. Values are redacted per §3.1.

A GitHub personal access token was provided by the operator for the sunshift project. Value redacted per §3.1. A Cloudflare API token with Workers, D1, KV, Pages, and Queues scope was provided and configured. Values redacted per §3.1. A Cloudflare Access Token (cfat_ prefix) was verified working with wrangler and saved to the credentials file. Value redacted per §3.1.

A BASS login credential (username LandonDavenport, password) was provided by the operator for recon purposes. Password value redacted per §3.1. The credential was used only for reconnaissance of the bass.docxtract.com site architecture.

The operator explicitly told the agent to stop experimenting with BASS click automation on a production system before a safe test path is established.

## Open questions

The address seeder fix (reducing CF Worker subrequests from ~130 parallel to ~52 sequential to stay within the 1000-subrequest limit) was deployed but not verified end-to-end. The primary open question is whether grey address dots now appear on the map when a territory is created with ZIP 31313.

The Smarty Autocomplete crawl per ZIP code yields only ~500 addresses. Whether that coverage is sufficient for the territories Brightspeed reps work in is unresolved. OpenAddresses bulk import was proposed as a supplemental source.

The operator asked about using Smarty US Reverse Geocoding as a territory-seeding approach. The decision to use OpenAddresses plus Smarty Street API instead was recommended but not explicitly confirmed as final.

The lifeparse project has an open question about whether 1080p video frames from the RealWear HMT-1 can meaningfully OCR monitor body text. The assessment is that headings, browser tabs, and large-font terminal text are readable but normal body text is not.

The `pat_classic_write` GitHub token stored in credentials was returning HTTP 401 Bad Credentials at the end of the session. The operator had not yet provided a replacement.

## People mentioned

LandonDavenport (Brightspeed BASS sales portal user account, used for recon; not a project team member).

## Dates and deadlines referenced

Session started 2026-05-06 and ended 2026-05-12. No explicit forward deadlines were set during the session.

## Thoughts not yet in the canvas

The operator explored a lifeparse concept: a RealWear HMT-1 wearable camera that captures video or stills every few seconds, sends frames to a server, runs OCR and Claude vision analysis, and produces a daily digest of everything readable in the wearer's field of view. This is a separate project from Knocklist and Sunshift. Key architectural decisions were discussed: HMT-1 blocks direct Camera2 API access for third-party apps and requires system intent-based capture; the plan uses continuous 30-minute video segments sliced server-side by ffmpeg rather than silent background capture; Cloudflare R2, Durable Objects, D1 with FTS5, and Claude Opus 4.7 are the proposed backend stack. A PLAN.md was written and committed to ~/projects/lifeparse/. A ClamAV scan of the cloned screenpipe repository found 1487 files and zero infections.

The operator discussed the feasibility of combining OpenAddresses free bulk data with Smarty Street API validation as a pipeline to seed every deliverable residential address in a territory into D1, then push them to the rep's device during a pre-flight sync. This pattern (grey dot on every house before any knocking begins) was confirmed as the intended UX but is not yet working due to the subrequest limit bug.

The operator asked about running the Knocklist PWA fully offline. The proposed approach is IndexedDB plus Service Worker background sync: pins drop instantly to local storage and sync silently when connectivity returns. Territory polygons, address data, and today's team pins are pre-cached during a morning pre-flight on WiFi. This design was discussed in detail but not yet built.

The operator asked about porting Knocklist features to Cloudflare edge. The conclusion was that D1 handles all relational CRUD, client-side ray-casting replaces PostGIS for point-in-polygon checks, pre-aggregated stats tables handle leaderboards, and Stripe webhooks work natively in Workers.

Sunshift (the BASS speed-entry frontend) was fully deployed to sunshift.xyz with a Preact + Vite frontend on CF Pages, a Hono API worker, a queue consumer using @cloudflare/playwright for Browser Run, D1 for storage, KV for address caching, and Cloudflare Queues for async order submission. The deployment was verified end-to-end: registration created a user in D1 and returned a session cookie through the live custom domain.

## Status changes implied

No canvas milestone slugs were referenced. The following status changes are implied by the session:

The Sunshift project moved from planned to deployed. Both the API worker and Pages frontend are live at sunshift.xyz with SSL active.

The Knocklist project moved from a freshly cloned solarknock fork to a working deployed application at knocklist.app with auth, org management, territory drawing, pin dropping, leaderboard, and Google Maps integration functional.

BASS integration was deprioritized from active development to shelved. The UI hiding of the BASS integration feature remains a pending task.

The address seeder was identified as broken (CF Worker subrequest limit) and a fix was deployed, but the fix is unverified. Seeder status is deployed-untested.

